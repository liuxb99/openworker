# Case 0004：O87 最新本機總控缺口修復進度

- 日期：2026-08-19（Asia/Taipei）
- Case：0004 / DWG → 結構辨識 → 3D → AI-OpenSees
- 固定機：DESKTOP-O87PJNR（O87）
- Workspace：D:\AI-Work\jobs\0004-DWG-TO-3D
- 原則：案例實跑遇到缺口就補產品缺口，不繞過、不以舊 GitHub business Action 假閉環。

## 1. 本輪目的

先讓 O87 通過最新版真正本機總控驗收，再回到 Case 0004 的 canonical next READY business step。

最新版控制面：

```text
OpenWorker :8787
  -> gtr-local-exec
  -> go-tool-runtime :8848 durable local-work queue
  -> 4 claim workers + 4 execution workers
  -> allowlisted local capability
```

GitHub Actions 只用於 code repair / deployment / verification，不作 Case business scheduler。

## 2. 已修缺口

### 缺口一：server API package 路徑漂移

修復 commit：`696c42b8c55fe0a9c2af144c9caa8fe9e65a843f`，`internal/api` 改回 canonical `pkg/api`。

### 缺口二：server.go stale import

修復 commit：`b8db6bf5ab2ab8adde587d29b2cf5737273b63e4`，移除未使用 `internal/profile`。

### 缺口三：local supervisor bootstrap 被 optional tool checkout 綁死

修復 commit：`62b5f4a9773b9e08c32b260a44e3215ed5e057e6`。

未明確指定且本機找不到 OpenWorker / ComfyX / ComfyX-Studio / OpenMAIC 時，只停用對應 capability，不再阻塞 :8848 supervisor；明確指定但無效仍 fail-closed。

### 缺口四：REAL runner unit test 被 installed authority 污染

修復 commit：`275577ba67e20b9a0fac3d6d66c0e6d5990e08c6`，generic queue unit test 隔離 `ProgramData`；production hostname / capability authority fail-closed 不放寬。

後續 `81f7082a2c65acf09780d44a1b60dbefd219ea6b`、`0f37b8096e8d1b62993544d7f1af247f1d7ff8eb` 又把 supervisor-status 測試與 REAL-host verification authority 對齊。

## 3. O87 最新 REAL 部署結果

O87 job `95912163907` / run `32198262403` 已完整成功：

- targeted tests PASS
- build PASS
- install PASS
- Bootstrap per-machine durable local queue PASS
- Tailscale ingress step PASS
- persistent local-work control plane PASS
- deployment receipt PASS

REAL log：

```text
GTR_LOCAL_WORK_RUNTIME_READY host=DESKTOP-O87PJNR
queue=http://127.0.0.1:8848
target=http://DESKTOP-O87PJNR:8848
mode=detached-wmi-process
parallel=4

GTR_LOCAL_CONTROL_PLANE_READY
host=DESKTOP-O87PJNR
queue=http://127.0.0.1:8848
target=http://DESKTOP-O87PJNR:8848
```

最新 persisted deployment receipt：`evidence/deployments/latest-local-executors-O87.json`，schema `gtr-local-executor-deployment/v4`，`queue_configured=true`，runtime mode=`detached-wmi-process`。

## 4. 缺口五：deployment success 尚未等價於 REAL 4+4 OPERATIONAL

### 發現

雖然上述 deployment workflow 全綠，但舊 workflow 的成功條件只驗：

- :8848 health
- authority receipt 存在
- queue/agent/executor process alive
- deployment receipt 成功寫回 Git

它沒有在 deployment success gate 中強制執行 `verify-gtr-true-local-supervisor.ps1`。

因此舊的：

```text
deployment success
```

只能證明 control plane 存活，不能證明：

```text
4 distinct claim workers
4 distinct executor slots
REAL parallel peak=4
route=LOCAL_SUPERVISOR
GitHub business execution=false
```

這違反 Case 0004 的 OPERATIONAL 驗收鐵律。

### 修復

repo：`liuxb99/go-tool-runtime`

commit：`22b20f3122111f6c5f29538673a3a14f8c8bf1ab`

`.github/workflows/deploy-local-executors.yml` 已新增 fail-closed step：

```text
Verify REAL four-slot local supervisor
```

直接執行：

```text
scripts/windows/verify-gtr-true-local-supervisor.ps1
```

只有同時滿足以下條件才允許 deployment receipt：

- `status=REAL_VERIFIED`
- `route_label=LOCAL_SUPERVISOR`
- `github_action_used_for_business_execution=false`
- `distinct_claim_worker_count >= 4`
- `distinct_executor_slot_count >= 4`

Deployment receipt 升級為 `gtr-local-executor-deployment/v5`，新增：

- `real_verified=true`
- `route_label=LOCAL_SUPERVISOR`
- `distinct_claim_worker_count`
- `distinct_executor_slot_count`
- `real_verification_receipt`
- `github_action_used_for_business_execution=false`

並新增 workflow path trigger：`scripts/windows/verify-gtr-true-local-supervisor.ps1`。

## 5. 當前狀態

已完成：

- [x] canonical pkg/api
- [x] stale import cleanup
- [x] supervisor 與 optional tool checkout 解耦
- [x] generic queue / supervisor status test isolation
- [x] O87 targeted tests REAL PASS
- [x] O87 build/install/bootstrap REAL PASS
- [x] O87 :8848 persistent control plane REAL PASS
- [x] deployment receipt v4 已落盤
- [x] 補上 deployment 必須 REAL 4+4 才成功的缺口

正在驗證：

- [ ] 新 O87 job `95913043029` 使用 `22b20f3...` 或後繼 main
- [ ] REAL 4 distinct claim workers
- [ ] REAL 4 distinct executor slots
- [ ] REAL parallel peak=4
- [ ] route=LOCAL_SUPERVISOR
- [ ] deployment receipt v5
- [ ] O87 = OPERATIONAL

通過後立即：

- [ ] 向 OpenWorker 查 Case 0004 live canonical next READY step
- [ ] 向 go-tool 查該 step capability / inputs / restrictions / negative knowledge
- [ ] 由 LOCAL_SUPERVISOR dispatch 下一個 REAL business work

## 6. 驗收鐵律

```text
source fixed != deployed
deployed != REAL 4+4 verified
process alive != OPERATIONAL
queue health != 4+4 verified
control path success != business artifact acceptance
```

只有 v5 REAL 4+4 deployment receipt 成立，Case 0004 才解除 business dispatch gate。
