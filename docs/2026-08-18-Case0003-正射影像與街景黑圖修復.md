# Case 0003 玉井橋：正射影像漏接與街景黑圖修復

日期：2026-08-18
狀態：REWORK / REAL 驗證中
固定機器：DESKTOP-UL7V2VV（UL7）
工作目錄：D:\AI-Work\jobs\0003-YUJING-BRIDGE
Work code：OWJ-20260816030152-03D90D

## 1. 本輪實體缺口

Case 0003 目前不能進入 Delivery PASS，原因不是 DTM、AOI 或 Blender，而是影像呈現鏈仍有兩個實體缺口：

1. SceneX Region Pack / terrain runtime 先前沒有消費既有 NLSC 正射影像能力，REAL browse 仍可能以純色 terrain material 呈現。
2. 街景四方向 PNG 雖存在且 SHA256 正確，但實體畫面可為近純黑；既有 Case driver 與 workflow 只驗證存在、非空與雜湊，沒有驗證語義可見性，因此黑圖可被錯誤接受並長期復用。

因此，本輪禁止重跑已正確的 DTM / AOI / Blender，只重跑 imagery / street-view / SceneX presentation 受影響階段。

## 2. 街景黑圖根因與 owning repo 修復

Owning repo：`liuxb99/Terrain_To_DXF`

已完成代碼修復：

- `27b02f0`：master panorama evidence 增加畫面語義可見性指標，包括平均亮度、亮度標準差、亮度範圍、非黑像素比例與 `semantic_visible`；近純黑影像 fail-closed。
- `e7c8a73`：新增永久回歸測試，確保黑圖不能再被視為成功產物。

Case 0003 的第二層復用缺口已於 AI-Engineering-OS imagery-only driver 補齊：舊 `streetview` 目錄在重跑前整體移入 `.openworker/quarantine/streetview/pre-semantic-<run-id>`，保留歷史證據但禁止復用；其後重新調用正式 `terrain.streetview.acquire`，四方向逐張計算亮度標準差、亮度範圍及非黑像素比例，任何一張不達標即 fail-closed。

## 3. 正射影像不是新能力，而是整合遺漏

既有 `AI-Engineering-OS` 已有 NLSC 正射影像主線：

- `POST /api/v1/jobs/{id}/site/acquire-imagery`
- NLSC `PHOTO2` job-scoped orthophoto acquisition。
- georeferenced orthophoto → Godot terrain texture。
- `POST /api/v1/jobs/{id}/scene/apply-imagery`

本次缺口是 Case 0003 進入新版 SceneX Region Pack 時漏接既有 imagery contract，不重新發明 downloader / mapper。

Owning repo：`liuxb99/SceneX`

已完成代碼修復：

- `22f69bf`：Region Pack 可消費既有 `scenex-textured-assembly/v1`，驗證 texture SHA256，並明確轉換舊 OS 與新版 Region Pack 的 local-Z 方向契約，避免南北翻轉。
- `ac5bc1e`：production terrain geometry adapter 實際載入外部正射影像、驗證 SHA256，並依 X/Z bounds 映射到 terrain；texture 或 mapping 不合法時 fail-closed。

## 4. Case 0003 imagery-only 實作

AI-Engineering-OS：

- `293bc433`：新增 `scripts/case0003_imagery_repair_ul7.py`。
- `7ffe42dc`：新增 `.github/workflows/case-0003-imagery-repair-ul7.yml`，固定 `[self-hosted, Windows, X64, UL7]` / `DESKTOP-UL7V2VV`，只執行影像受影響階段。
- `6bd2ea1`：修正 SceneX handoff contract；`imagery/scenex-textured-assembly.json` 根節點現在就是 canonical `scenex-textured-assembly/v1`，不再誤寫 HTTP response wrapper。同時 `imagery/nlsc-site-imagery.json` 根節點為 canonical `site-imagery/v1`。

workflow 的 push path 同時包含 driver 與 workflow 本身，因此 `6bd2ea1` 已再次觸發 UL7 REAL imagery-only 驗證。此處只記錄「已觸發」，在取得 Actions / 工作目錄實體證據以前不得記為 PASS。

## 5. 新的 fail-closed 驗收規則

下一輪 REAL 必須同時符合：

1. 四張 street-view direction image 均存在、非空、SHA 正確。
2. 四張 direction image 必須具有 semantic visibility evidence；近純黑、近單色、無有效結構不得 PASS。
3. 舊街景若沒有新版 semantic evidence，不得 reuse，必須重新取得。
4. NLSC orthophoto 必須有 provider / georeference / SHA provenance。
5. SceneX Region Pack 必須明確引用 orthophoto texture 與映射 bounds。
6. SceneX REAL browse 必須實際看見有內容的正射地表，而不是純色材質。
7. imagery texture SHA 必須與 assembly / Region Pack / workspace evidence 一致。
8. 若貼圖座標方向錯位、翻轉或沒有覆蓋 terrain，視為 TOOL_GAP。
9. 最終 SceneX screenshot 仍需 ChatGPT 實體視覺審查；機械 gate PASS 不等於語義 PASS。
10. 玉井橋案例最終若要求橋梁本體可見，而 SceneX 仍只有 terrain，不能因正射影像 PASS 而關閉案例。

## 6. 本輪執行順序

`invalidate old black street-view cache`
→ `REAL street-view reacquire/capture`
→ `semantic visibility gate`
→ `NLSC PHOTO2 4096x4096`
→ `canonical scenex-textured-assembly/v1`
→ `SceneX Region Pack --require-imagery`
→ `SceneX REAL browse on UL7`
→ `physical screenshot review`
→ `fresh OpenWorker review revision`
→ `Google Drive exact revision review`
→ `WorkLedger`
→ `Delivery Revision`
→ `Open-Case0003-SceneX.bat --check-only`
→ `human double-click BAT and inspect actual SceneX result`

## 7. 不得回退

- 不因 imagery 缺口重跑 DTM / AOI / 已驗證 Blender terrain。
- 不以「PNG 可解碼、非空、SHA 正確」代替街景可見性。
- 不以 SceneX 純色 terrain 代替正射影像。
- 不覆寫舊 `rev_0851...` 來假裝含有新成果；修復後必須建立 fresh immutable review revision。
- BAT 存在不代表 Case CLOSED；BAT 是最終實體人工查看入口，仍需 Drive / WorkLedger / Delivery 閉環。

## 8. 目前狀態（2026-08-18 10:01 +08）

代碼層：

- 街景 source semantic gate：IMPLEMENTED。
- SceneX orthophoto production bridge：IMPLEMENTED。
- Case 0003 舊黑街景 cache invalidation：IMPLEMENTED。
- canonical imagery / textured assembly handoff：IMPLEMENTED。
- UL7 imagery-only workflow：IMPLEMENTED，REAL 已由 `6bd2ea1` push path 再次觸發，等待實體結果。
- DTM / AOI / Blender：保持既有已驗證成果，不重跑。
- 新 SceneX / street-view 實體成果審查：WAITING REAL RESULT。
- fresh review / Drive / WorkLedger / Delivery / BAT 最終驗收：等待影像與 SceneX REAL 視覺 PASS 後接續。
