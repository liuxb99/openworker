# 案例 0002 阿拉丁神燈：REAL 逐步操作紀錄

建立時間：2026-08-18 10:42 Asia/Taipei

本文件只記實際操作時間線；架構與產品規則見 `0002-阿拉丁神燈-REAL完整閉環手冊.md`。

## 2026-08-18

### 10:27 — 使用者校正問題處理原則

指令：有問題問 OpenWorker 和 go-tool。

執行規則固定為：

```text
不知道 current state / host / dependency / acceptance → 問 OpenWorker
不知道 capability / queue / Action / inputs / readiness / status / cancel → 問 go-tool
兩者都沒有答案 → 才列系統缺口
```

### 10:28 — 問 OpenWorker：如何取得 durable current state

檢查 OpenWorker `scripts/case_worklist_action.py`。

答案：存在正式 `show --workspace-root <workspace>`，直接呼叫 `CaseWorklistRuntime.load()` 並輸出 durable Worklist JSON。

結論：Git `case-worklists/0002.json` 只能作模板；真實 current step / assigned_host 必須由本機 durable Worklist 回答。

### 10:30 — 問 OpenWorker：舊 0002 run 能否作 current harness rerun

讀取歷史案例紀錄：

- `31923007861`：舊 Shot-1 REAL provenance closure；
- `31925346602` / job `95112376375`：舊工作目錄 P0 REAL PASS；
- `31928045624`：generic runner 被 UL7 接單後 COMPUTERNAME gate skip。

答案：以上均不是新版 `0002-025` PPT governed harness，不得冒充 current run 或直接拿來作 025 rerun。

### 10:32 — 發現 OpenWorker-first 順序缺口

檢查 `go-tool-runtime/cmd/e2e-0002-dispatch/main.go`。

發現：程式在讀 OpenWorker 之前先硬編碼：

```text
COMPUTERNAME == DESKTOP-ODAQN0D
```

之後才讀 durable Worklist 的 `assigned_host`。

判定：違反「先問 OpenWorker」。owning repo=`go-tool-runtime`。

### 10:33 — 修 go-tool-runtime harness

commit：`7f901fc0faba0b0ff229b6a898143f6737b7023a`

修後順序：

```text
load D:\AI-Example\0002\.openworker\case-worklist.json
→ 00-openworker-current.json
→ case_id == 0002
→ assigned_host nonempty
→ COMPUTERNAME == assigned_host
→ canonical next step
→ go-tool runtime
→ /agent/query
```

並將 readiness 錯誤文字由 `not ready on ODA` 改為 `not ready on assigned host`。

狀態：`IMPLEMENTED / NOT LIVE VERIFIED`。

### 10:34 — 檢查修復 commit 是否觸發 Operator

`7f901fc0...` 修改 `cmd/e2e-0002-dispatch/**`，符合現行 Operator workflow push path。

檢查：

- `evidence/routes/latest-0002-route-slot-1.json` → Not Found
- slot-2 → Not Found
- slot-3 → Not Found
- `evidence/latest-0002-dispatch.json` → Not Found
- combined commit status → 無可見 status context

結論：目前沒有機器 evidence；不得猜成「未觸發」「排隊」「runner 離線」或其他原因。

### 10:37 — 問 go-tool：能否回答 current Operator queue

檢查 `internal/execution/queue_admin.go` 與 `cmd/gtr-actions-queue`。

go-tool 的答案邊界：

- queue list / drain 必須經已註冊 capability；
- workflow-scoped query 使用 capability 綁定的 repository/workflow；
- 不允許 caller 任意傳 repo/workflow；
- 可查 queued / in_progress / waiting / pending；
- 可 preserve 指定 run id；
- 可 cancel 後驗證 queue 是否 clean。

發現：當前 `Operator E2E 0002 ComfyX-Studio` 沒有對應 admin capability，因此 go-tool 無法合法回答「這條 Operator workflow 現在是否 active」。

判定：明確 go-tool capability registry 缺口。

### 10:38 — 補 0002 Operator admin capability

新增：

`capabilities.d/openworker-case0002-operator-admin.yaml`

capability：

`openworker.case0002.operator.admin`

綁定：

```text
repository = liuxb99/go-tool-runtime
workflow = operator-e2e-0002-comfyx-studio.yml
ref = main
execution.mode = queue_admin
```

此 capability 只允許管理/query，不做 storyboard/image/video/PPT 業務。

commit：`f998bffa72fdd9b68431f8223f137782f6af3d1a`

### 10:39 — 補永久 regression test

新增 `internal/config/openworker_case0002_operator_admin_fragment_test.go`。

驗證：

- capability id 精確；
- repository/workflow/ref 精確；
- execution mode 必須 `queue_admin`；
- 不得出現 business inputs。

commit：`1971fe9d6450630feea43388551032e23106e52c`

狀態：`IMPLEMENTED / TEST FILE ADDED / CI NOT YET LIVE VERIFIED`。

### 10:40 — 讓 go-tool 自己做一次只讀 queue query

新增 GitHub-hosted workflow：

`.github/workflows/query-case0002-operator-admin.yml`

目的不是產品執行，只是正式執行：

```text
go run ./cmd/gtr-actions-queue
  --capability openworker.case0002.operator.admin
  --workflow-scoped=true
```

不帶 `--cancel-active`，因此只查詢，不取消任何任務；輸出應寫：

`evidence/ops/latest-case0002-operator-queue.json`

commit：`d2bcd80a072e0ac842d9f6ec6ac0eb22434396eb`

### 10:41 — 查詢 go-tool queue evidence

結果：

`evidence/ops/latest-case0002-operator-queue.json` → Not Found。

`d2bcd80a...` combined status → 無可見 status context。

結論：目前只證明「只讀 queue query 的正式入口已建立，但本輪沒有可見機器回執」。不能猜原因。

### 10:42 — 當前真實狀態

產品步驟：`0002-025 NOT STARTED / NOT LIVE VERIFIED`。

已完成的系統修正：

1. OpenWorker-first host authority；
2. go-tool `/agent/query` 可看 Action capability directory；
3. 0002 Operator queue 有正式 admin capability；
4. 只讀 queue query workflow 已建立。

仍缺的 REAL evidence：

```text
OpenWorker durable current JSON
01-agent-query.json
operator queue query JSON
target presentation.openmaic run id
storyboard.pptx
manifest
receipt
physical media_count=0
OpenWorker PASS 025 → next 027
```

下一個操作仍遵循：有狀態問題問 OpenWorker；有 Action/queue 問題問 go-tool；不由模型猜。