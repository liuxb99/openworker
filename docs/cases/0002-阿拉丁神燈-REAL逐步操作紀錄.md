# 案例 0002 阿拉丁神燈：REAL 逐步操作紀錄

建立時間：2026-08-18 10:42 Asia/Taipei

本文件只記實際操作時間線；架構與產品規則見 `0002-阿拉丁神燈-REAL完整閉環手冊.md`。

## 2026-08-18

### 10:27 — 使用者校正問題處理原則

指令：有問題問 OpenWorker 和 go-tool。

執行規則固定為：

```text
不知道 current state / host / dependency / acceptance → 問 OpenWorker
不知道 capability / queue / Action / inputs / readiness / status / cancel → 問 go-tool
兩者都沒有答案 → 才列系統缺口
```

### 10:28 — 問 OpenWorker：如何取得 durable current state

檢查 OpenWorker `scripts/case_worklist_action.py`。

答案：存在正式 `show --workspace-root <workspace>`，直接呼叫 `CaseWorklistRuntime.load()` 並輸出 durable Worklist JSON。

結論：Git `case-worklists/0002.json` 只能作模板；真實 current step / assigned_host 必須由本機 durable Worklist回答。

### 10:30 — 問 OpenWorker：舊 0002 run 能否作 current harness rerun

讀取歷史案例紀錄：

- `31923007861`：舊 Shot-1 REAL provenance closure；
- `31925346602` / job `95112376375`：舊工作目錄 P0 REAL PASS；
- `31928045624`：generic runner 被 UL7 接單後 COMPUTERNAME gate skip。

答案：以上均不是新版 `0002-025` PPT governed harness，不得冒充 current run 或直接拿來作 025 rerun。

### 10:32 — 發現 OpenWorker-first 順序缺口

檢查 `go-tool-runtime/cmd/e2e-0002-dispatch/main.go`。

發現：程式在讀 OpenWorker 之前先硬編碼 `COMPUTERNAME == DESKTOP-ODAQN0D`，之後才讀 durable Worklist 的 `assigned_host`。

判定：違反「先問 OpenWorker」。owning repo=`go-tool-runtime`。

### 10:33 — 修 go-tool-runtime harness

commit：`7f901fc0faba0b0ff229b6a898143f6737b7023a`

修後順序：

```text
load D:\AI-Example\0002\.openworker\case-worklist.json
→ 00-openworker-current.json
→ case_id == 0002
→ assigned_host nonempty
→ COMPUTERNAME == assigned_host
→ canonical next step
→ go-tool runtime
→ /agent/query
```

狀態：`IMPLEMENTED / NOT LIVE VERIFIED`。

### 10:34 — 檢查修復 commit 是否觸發 Operator

`7f901fc0...` 修改 `cmd/e2e-0002-dispatch/**`，符合現行 Operator workflow push path。

當時尚未看到 route/dispatch evidence，因此不猜觸發狀態。

### 10:37 — 問 go-tool：能否回答 current Operator queue

檢查 `internal/execution/queue_admin.go` 與 `cmd/gtr-actions-queue`。

go-tool 的答案邊界：queue list/drain 必須經已註冊 capability；workflow-scoped query 使用 capability 綁定的 repository/workflow；不允許 caller 任意傳 repo/workflow；可查 queued/in_progress/waiting/pending；可 preserve/cancel/verify。

發現：當前 Operator 沒有 admin capability，因此 go-tool 無法合法回答 queue 狀態。

### 10:38 — 補 0002 Operator admin capability

新增 `openworker.case0002.operator.admin`，commit：`f998bffa72fdd9b68431f8223f137782f6af3d1a`。

### 10:39 — 補永久 regression test

commit：`1971fe9d6450630feea43388551032e23106e52c`。

### 10:40 — 讓 go-tool 自己做一次只讀 queue query

新增 `.github/workflows/query-case0002-operator-admin.yml`，commit：`d2bcd80a072e0ac842d9f6ec6ac0eb22434396eb`。

### 10:41 — queue evidence 尚未出現

`evidence/ops/latest-case0002-operator-queue.json` 尚未出現；不猜原因。

### 10:43 — 找到真正 current governed harness run

搜尋 go-tool-runtime commit history 時，發現機器 evidence commit：

`51a2b3fd9cf6cadc2a8af2fccf52d808f1c4882b` — `evidence(0002): publish route slot 2`。

該 evidence 明確給出：

```text
harness_run_id = 32091092500
harness_run_attempt = 1
matrix_slot = 2
runner_name = DESKTOP-UL7V2VV-R008
computer_name = DESKTOP-UL7V2VV
route_run = false
source_sha = 7f901fc0faba0b0ff229b6a898143f6737b7023a
```

因此 `32091092500` 是目前正式 `Operator E2E 0002 ComfyX-Studio` harness run。

### 10:44 — 查 run `32091092500` jobs

最新 attempt jobs：

- `95573238814` / e2e(3) / failure
- `95573238819` / e2e(2) / success，非 ODA，產品步驟 skip
- `95573238910` / e2e(1) / failure

讀取 `95573238814` logs 後確認：

```text
Runner = DESKTOP-ODAQN0D-R008
Machine = DESKTOP-ODAQN0D
```

所以 e2e(3) 確實是 ODA。

### 10:45 — ODA 原始失敗根因

ODA job 在第一個 `actions/checkout@v4` 就失敗，尚未進 OpenWorker/go-tool：

```text
EBUSY: resource busy or locked
rmdir C:\github-runners\go-tool-runtime\_work\go-tool-runtime\go-tool-runtime\gtr-bootstrap-31998928453
```

因此 025 不是業務失敗，而是 runner workspace hygiene failure。

### 10:46 — 正式 rerun ODA job

透過 GitHub Actions write permission rerun job `95573238814`。

GitHub 建立新 job：`95575621633`。

### 10:48 — rerun 終態

`95575621633` completed/failure。再次讀 log，仍為相同 ODA 機器、相同 checkout EBUSY、相同鎖目錄：

`gtr-bootstrap-31998928453`

這證明不是偶發 checkout race；舊工作樹持續被某程序持有。

### 10:49 — 問 go-tool 是否有 runner workspace cleanup 正式能力

搜尋 go-tool-runtime runner/workspace/cleanup/EBUSY/bootstrap 等 capability/工具，沒有找到可安全清理自己 runner checkout 的正式 capability。

依規則，兩邊都沒有現成答案後才形成系統缺口：

```text
GAP = self-hosted runner checkout workspace hygiene
OWNING REPO = go-tool-runtime workflow
```

禁止直接強殺未知進程或刪鎖目錄。

### 10:50 — 安全 owning fix：隔離 checkout

決策：不碰舊 `gtr-bootstrap-31998928453`，不強殺進程；新增每個 run/attempt/slot 唯一 checkout 目錄，避免 actions/checkout 清理舊被鎖工作樹。

新增 workflow：

`.github/workflows/operator-e2e-0002-governed-v2.yml`

commit：`7e0e31e894d747a2ec48704c526462f4481777e8`

核心：

```text
GTR_SOURCE_REL = source-<run>-<attempt>-<slot>
OPENWORKER_REL = openworker-<run>-<attempt>-<slot>
actions/checkout clean=false
```

業務鏈未改：

```text
route by COMPUTERNAME
→ checkout OpenWorker authority
→ go run ./cmd/e2e-0002-dispatch
→ OpenWorker durable current
→ go-tool /agent/query
→ capability detail/readiness
→ owning Action
→ persistent receipt/QC
→ Worklist acceptance
```

v2 winner會發布：

`evidence/latest-0002-governed-v2.json`

狀態：`IMPLEMENTED / WAITING REAL MACHINE POINTER`。

### 10:51 — v2 初次機器證據檢查

最新 go-tool-runtime commits 尚只有 `7e0e31e8...` workflow 提交，尚未看到 `evidence(0002): publish governed v2 pointer`。

因此不能聲稱 v2 已接單或 025 已開始。

### 10:51 — 當前產品狀態

`0002-025 NOT STARTED / NOT LIVE VERIFIED`。

目前最新 blocker 已由模糊「控制面」縮小成：

```text
舊 governed harness ODA checkout workspace EBUSY
→ 已新增 isolated checkout v2
→ 等第一份 v2 machine pointer
```

取得 v2 pointer 後，必須繼續追到：

```text
00-openworker-current.json
01-agent-query.json
presentation.openmaic target run
storyboard.pptx + manifest + receipt
physical ppt/media count = 0
OpenWorker PASS 025
canonical next = 027
```
