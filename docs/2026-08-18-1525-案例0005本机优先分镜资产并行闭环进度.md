# 案例 0005 白雪公主：本机优先分镜资产并行闭环进度

时间：2026-08-18 15:25 +08:00

状态：030/040/050 IMPLEMENTED — BLOCKED BEFORE BUSINESS BOOTSTRAP BY ODA TRANSPORT EVIDENCE GAP

## 当前事实

Case 0005 的权威 Worklist 为 `case-worklists/0005.json`，固定机器 `DESKTOP-ODAQN0D`，业务工作目录 `D:\AI-Work\jobs\0005-SNOW-WHITE`。

当前 DAG：

- `0005-010`：Comfyx-Studio Director preproduction。
- `0005-020`：生成 storyboard request 与 visual asset requirements。
- `0005-025`：OpenMAIC 生成无图文字分镜 PPTX。
- `0005-027`：等待用户批准文字分镜 PPTX。
- `0005-030`：批准后生成 REAL character master images。
- `0005-040`：与 030 并行生成 REAL scene concept images。
- `0005-050`：030/040 join 后，在一个原子 local capability 内生成 REAL shot storyboard images、持久化每个 IMAGE receipt，再 bind 回 storyboard request。
- `0005-055`：生成 illustrated storyboard PPTX。
- `0005-057`：第二次用户批准。
- 后续进入逐 shot REAL video fan-out、finalize、WorkLedger、Drive review、LLM review、Delivery Revision。

## 已关闭的缺口

### 1. 移除 nested GitHub business execution

旧 `case0005_parallel_bootstrap.ps1` 会在 OpenWorker durable job 内再次调用 GitHub workflow，违反 local-first。现已改为：GitHub Action 只做一次 transport/bootstrap；真正业务执行路径是：

`coworker.case0005_controller -> OpenWorker Go durable job -> gtr-local-exec -> ComfyX / Comfyx-Studio / OpenMAIC`

### 2. 失败 evidence 必须可审计

v3 bootstrap 已改为 bootstrap `continue-on-error`，observe 与 bounded evidence commit 使用 `always()`；设计目标是即使 bootstrap 失败也先记录 node/jobs/Worklist/controller/error，再 fail-closed。

### 3. 030/040 权威 role 分支

go-tool `image.comfyx.storyboard-real` 已支持 `role` 批量执行，只允许：

- `character_master`
- `scene_concept`
- `shot_storyboard`

asset_id 一律从真实 `visual-assets/requirements.json` 取得，不猜 ID，不写死 ComfyUI graph/model/sampler。

`0005-030` 映射 `character_master`，验收 `character_receipts / character_images / character_sha256`。

`0005-040` 映射 `scene_concept`，验收 `scene_receipts / scene_images / scene_sha256`。

030 与 040 仍是两个独立 OpenWorker durable jobs，可占不同 local slot 并行；没有第二套 scheduler。

### 4. 050 REAL shot image + bind 原子闭环

真实 Studio bind contract 要求 receipt **文件路径**，不是内存 JSON。因此新增 go-tool capability：

`comfyx-studio.storyboard.real-bind`

一个 OpenWorker durable job 内依次：

1. 过滤 `role=shot_storyboard`。
2. 调 ComfyX REAL IMAGE。
3. 校验每个 workspace artifact / SHA256。
4. 把每个 receipt 写入 `evidence/storyboard-shot-images/`。
5. 把 receipt_relpaths 交给既有 `comfyx-studio.storyboard.bind`。
6. bind 成功且 bound request 实体存在才返回 completed。

Worklist revision 已升为 4，`0005-050.allowed_actions` 改为单一原子 capability `comfyx-studio.storyboard.real-bind`，避免 Worklist 记录 A、实际执行 B 的身份漂移。

## 本轮关键提交

- `fa092db9de0a2d5841b87ba751beb7dedc830699` — go-tool role batch IMAGE。
- `3dfc90ac9555422f6c78f0825fe2962c094d8f37` — Case 0005 专用 controller。
- `07b1a09aef2b679d1af442f03fd27e2db5f91245` — bootstrap 切到专用 controller。
- `390314412cdd70bf0434840c949f0407d0b5e131` / `3835deca32cb81fb00df2ffc0914d8e44e22fbcd` — Case 0005 controller tests。
- `1842b47b18853ba1246049bb6cbcbb2540721144` — REAL shot image + bind composite。
- `55a3ad93467d06fb7c96b01de02abb5b17f072f3` — 注册 composite capability。
- `fc48389275f39abc1416480f18a4991a3c7a6a68` — Worklist revision 4 / 050 原子 capability。
- `fae72131f825d67f79ea6827244a467c1a3b547c` — Case 0005 controller 接 050。
- `c8374e15ee3d4f8b326a4ec4e4a66beb0e2275d0` — go-tool role batch tests。

## 当前真实阻塞点

已多次用带 `[case0005-parallel-v3]` 的 main push 触发 ODA transport，但截至本文件更新：

- `case-evidence/0005` 在 GitHub 仍不存在。
- 没有 Case 0005 evidence-bot commit。
- 同期其他机器/案例仍有正常 Git 提交，因此不能归因于 GitHub repository 本身。

因此当前不能宣称 `0005-010` 已 accepted / running / succeeded。现有事实只足以判定：**Case 0005 阻塞发生在 OpenWorker business bootstrap 建立 durable fact 之前，优先检查 ODA self-hosted runner / transport 是否实际接单。**

在取得 ODA evidence 前，不再继续猜测或修改 ComfyX 业务算法。

## 恢复后的自动接续点

ODA transport 一旦恢复：

`010 -> 020 -> 025 -> 027` 自动 local-first 接续。

到 `0005-027` 必须停下，向用户交付并展示**无图文字分镜 PPTX**，等待明确批准；不得自动跨越审批门。批准后才允许 030 与 040 并行，随后 050 原子 REAL shot image + bind。

## 验收边界

`Action completed` 不等于 `Case step succeeded`。Case 0005 每一步只接受 OpenWorker Worklist durable state + localexec REAL receipt + artifact/hash/QC evidence。
