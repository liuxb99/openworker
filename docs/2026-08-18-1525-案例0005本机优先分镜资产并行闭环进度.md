# 案例 0005 白雪公主：本机优先分镜资产并行闭环进度

时间：2026-08-18 15:25 +08:00

状态：IMPLEMENTING — WAITING FOR ODA DURABLE EVIDENCE

## 当前事实

Case 0005 的权威 Worklist 为 `case-worklists/0005.json`，固定机器 `DESKTOP-ODAQN0D`，业务工作目录 `D:\AI-Work\jobs\0005-SNOW-WHITE`。

当前 DAG 已明确：

- `0005-010`：Comfyx-Studio Director preproduction。
- `0005-020`：生成 storyboard request 与 visual asset requirements。
- `0005-025`：OpenMAIC 生成无图文字分镜 PPTX。
- `0005-027`：等待用户批准文字分镜 PPTX。
- `0005-030`：批准后生成 REAL character master images。
- `0005-040`：与 030 并行生成 REAL scene concept images。
- `0005-050`：030/040 join 后生成 shot storyboard images 并 bind 回 storyboard request。
- 后续再进入 illustrated PPTX、第二次批准、逐 shot REAL video fan-out、finalize、WorkLedger、Drive review、LLM review、Delivery Revision。

## 本轮发现的缺口

旧 `case0005_parallel_bootstrap.ps1` 会在 OpenWorker durable job 内再次调用 GitHub workflow，违反 local-first 原则。已经改为：GitHub Action 只做一次 transport/bootstrap；真正业务执行必须由 `coworker.case_controller -> OpenWorker Go durable job -> gtr-local-exec -> ComfyX/Comfyx-Studio/OpenMAIC` 完成。

第二个缺口是失败证据：旧 v3 bootstrap 一旦失败，后续 observe/commit 会被跳过，导致 `case-evidence/0005` 完全没有可审计失败事实。v3 已改为 bootstrap `continue-on-error`，observe 与 bounded evidence commit 使用 `always()`，先持久化失败原因，再 fail-closed。

截至本文件时间，Case 0005 尚未看到 ODA evidence-bot 回推，因此不能宣称 `0005-010` 已成功；只能确认 transport 提交已进入 `main`。

## 030/040 权威资产分类

Comfyx-Studio `BuildStoryboardVisualAssetPlan` 已定义稳定 role：

- `character_master`
- `scene_concept`
- `shot_storyboard`

并且 shot 的 `reference_asset_ids` 会引用对应 character master 与 scene concept。因此 controller 不需要猜 asset id 或把 ComfyUI graph/model 写死：

1. `0005-030` 从真实 `visual-assets/requirements.json` 过滤 `role=character_master`，批量交给 `image.comfyx.storyboard-real`。
2. `0005-040` 同理过滤 `role=scene_concept`，与 030 作为两个独立 OpenWorker durable jobs 并行。
3. 两支都 PASS 后，`0005-050` 再处理 `role=shot_storyboard`，并调用 `comfyx-studio.storyboard.bind`。
4. IMAGE 的具体 workflow/model/sampler 仍由 ComfyX 知识图谱与执行器权威决定；OpenWorker 只传业务意图、asset_id、workspace 与机器约束。

## 验收边界

`Action completed` 不等于 `Case step succeeded`。Case 0005 每一步只接受 OpenWorker Worklist durable state + localexec REAL receipt + artifact/hash/QC evidence。

当前下一步：补齐 `0005-030 / 0005-040` 的 controller batch 逻辑与 acceptance mapper；随后继续等 ODA 回执，并以真实失败/成功 evidence 驱动下一轮。
