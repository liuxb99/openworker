# 案例 0005 白雪公主：本机优先分镜资产并行闭环进度

时间：2026-08-18 15:25 +08:00

状态：IMPLEMENTING — ODA LOCAL-FIRST RETRY TRIGGERED

## 当前事实

Case 0005 的权威 Worklist 为 `case-worklists/0005.json`，固定机器 `DESKTOP-ODAQN0D`，业务工作目录 `D:\AI-Work\jobs\0005-SNOW-WHITE`。

当前 DAG 已明确：

- `0005-010`：Comfyx-Studio Director preproduction。
- `0005-020`：生成 storyboard request 与 visual asset requirements。
- `0005-025`：OpenMAIC 生成无图文字分镜 PPTX。
- `0005-027`：等待用户批准文字分镜 PPTX。
- `0005-030`：批准后生成 REAL character master images。
- `0005-040`：与 030 并行生成 REAL scene concept images。
- `0005-050`：030/040 join 后生成 shot storyboard images 并 bind 回 storyboard request。
- 后续再进入 illustrated PPTX、第二次批准、逐 shot REAL video fan-out、finalize、WorkLedger、Drive review、LLM review、Delivery Revision。

## 本轮发现并已处理的缺口

旧 `case0005_parallel_bootstrap.ps1` 会在 OpenWorker durable job 内再次调用 GitHub workflow，违反 local-first 原则。已经改为：GitHub Action 只做一次 transport/bootstrap；真正业务执行必须由 `coworker.case0005_controller -> OpenWorker Go durable job -> gtr-local-exec -> ComfyX/Comfyx-Studio/OpenMAIC` 完成。

第二个缺口是失败证据：旧 v3 bootstrap 一旦失败，后续 observe/commit 会被跳过，导致 `case-evidence/0005` 完全没有可审计失败事实。v3 已改为 bootstrap `continue-on-error`，observe 与 bounded evidence commit 使用 `always()`，先持久化失败原因，再 fail-closed。

第三个缺口是 027 后的角色资产分支没有 materialize。现已完成：

- go-tool `image.comfyx.storyboard-real` 支持 `role` 批量执行；只允许 `character_master / scene_concept / shot_storyboard`，并从真实 `visual-assets/requirements.json` 取得 asset_id。
- Case 0005 使用专用 `coworker.case0005_controller`，不污染其他案例的通用 controller。
- `0005-030` 映射 `role=character_master`，返回 `character_receipts / character_images / character_sha256`。
- `0005-040` 映射 `role=scene_concept`，返回 `scene_receipts / scene_images / scene_sha256`。
- 两个 step 仍是两个独立 OpenWorker durable jobs，可占不同 local agent slot 并行；没有新增第二套 scheduler。

关键提交：

- go-tool role batch：`fa092db9de0a2d5841b87ba751beb7dedc830699`
- Case 0005 专用 controller：`3dfc90ac9555422f6c78f0825fe2962c094d8f37`
- bootstrap 切换专用 controller：`07b1a09aef2b679d1af442f03fd27e2db5f91245`

## 030/040 权威资产分类

Comfyx-Studio `BuildStoryboardVisualAssetPlan` 已定义稳定 role：

- `character_master`
- `scene_concept`
- `shot_storyboard`

shot 的 `reference_asset_ids` 会引用对应 character master 与 scene concept。因此 controller 不猜 asset id，也不把 ComfyUI graph/model 写死。

IMAGE 的具体 workflow/model/sampler 仍由 ComfyX 知识图谱与执行器权威决定；OpenWorker 只传业务意图、role/asset_id、workspace 与机器约束。

## 验收边界

`Action completed` 不等于 `Case step succeeded`。Case 0005 每一步只接受 OpenWorker Worklist durable state + localexec REAL receipt + artifact/hash/QC evidence。

本次文档提交同时带 `[case0005-parallel-v3]`，用于再次触发 ODA bootstrap transport。下一判断点是 `case-evidence/0005/local-parallel-v3.json` 与 ODA Worklist durable state；未取得这些事实之前，不宣称 `0005-010` 成功。
