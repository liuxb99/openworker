# Case 0004：本機總控遷移實作進度

- 日期：2026-08-18 14:38（Asia/Taipei）
- 固定機：`DESKTOP-O87PJNR`
- Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`
- 基準手冊：`docs/2026-08-18-1412-Case0004-本機總控並行Action遷移與執行手冊.md`

## 已完成

1. Case 0004 架構已由 GitHub Actions scheduler 主線遷移為 OpenWorker local durable execution 主線。
2. `DWG_todo` 已新增純本機 Story Index operator：`scripts/operator_dwg_story_index_local.ps1`。
   - 不呼叫 `gh workflow run`。
   - 直接 build `dwg-editor` / OpenCAD。
   - 直接啟本機 CAD HTTP service。
   - 直接呼叫 `cad.build_story_index` / `cad.render_story_viewports`。
   - receipt 記錄 `OPENWORKER_JOB_ID` / `OPENWORKER_AGENT_SLOT`。
   - DWG_todo commit：`6f72a76a18036e74912c9449241502e7449f63f7`。
3. OpenWorker 已新增 Case0004 本機 durable submitter：`scripts/case0004_submit_story_index_local.ps1`。
   - 先查本機 OpenWorker node / cluster agents。
   - 先問本機 go-tool `/agent/query`。
   - fail-closed 驗證 capability `dwg.story_index.execute.case-worklist`。
   - 使用既有 Action checkout 目錄，不 clone 第二份 runtime repo。
   - POST `/v1/jobs`，Action/GitHub 不持有長任務。
   - OpenWorker commit：`491611bd8b3c6c4e7b5efe17bad0ca1cc7a2a87e`。
4. go-tool business capability 已改為 local-first guidance。
   - GitHub workflow 僅 legacy / emergency transport。
   - commit：`8ea4226dcf275aeb95a461d5b89b6e1e84802926`。
5. go-tool queue-admin 已改為 OpenWorker durable queue authority 優先。
   - commit：`99bed66f22137658f160c8858f1173b2c57a92dd`。
6. Case0004 Worklist manifest 已升 revision 5。
   - `parallel_policy.authority = openworker-local-supervisor`
   - `max_local_slots = 4`
   - `github_business_scheduler = false`
   - `0004-047` 改為 `fanout`
   - `0004-049` 改為 `join`
   - commit：`cd9a61e8a75f9757913664b957e813ad33bc2abd`。

## 已確認的 REAL Case 狀態

既有 O87 durable evidence 顯示：

- `0004-010` PASSED
- `0004-020` PASSED
- `0004-030` PASSED
- `0004-040` PASSED
- canonical next step：`0004-045`
- `0004-045`：READY
- primary DWG SHA256：`aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6`
- overview：`D:\AI-Work\jobs\0004-DWG-TO-3D\dwg\exports\default\visual-search\case0004-overview.png`
- overview SHA256：`5cee03340cbbcad51e412b46b85bda9dcaac22b193586b953bbfd5134039103e`

因此禁止重跑 0004-010 ~ 0004-040；新版直接承接 `0004-045`。

## 當前唯一必要輸入

`cad.build_story_index` 需要 Supervisor 從 REAL overview 做多模態樓層定位，形成：

- authoritative `camera_bounds`
- `image_width` / `image_height`
- per-story `pixel_bounds`
- `confidence`
- `visual_basis`

倉庫目前沒有可驗證的 REAL localization receipt，因此不得猜造樓層框。

## 下一執行

取得 overview 圖像後：

```text
ChatGPT / Local LLM visual localization
-> case0004-story-index-params.json
-> scripts/case0004_submit_story_index_local.ps1
-> OpenWorker durable job 0004-045
-> Story Index REAL artifact
-> 0004-047 per-story durable fan-out
-> 0004-049 join + multimodal review
```

## 狀態

**LOCAL EXECUTION PATH IMPLEMENTED — CASE0004 0004-045 READY — WAITING ONLY FOR REAL OVERVIEW VISUAL LOCALIZATION INPUT**
