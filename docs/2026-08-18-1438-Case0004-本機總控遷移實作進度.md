# Case 0004：新版本機總控遷移實作進度

- 更新：2026-08-18 14:19（Asia/Taipei）
- 固定機：`DESKTOP-O87PJNR`
- Workspace：`D:\AI-Work\jobs\0004-DWG-TO-3D`
- 主手冊：`docs/2026-08-18-1412-Case0004-本機總控並行Action遷移與執行手冊.md`

## 新版基準

Case 0004 已正式採用：

```text
OpenWorker = 本機總控 / durable state / queue / slots / process / progress
go-tool    = 工具 capability / entrypoint / constraints / negative knowledge
GitHub     = source + docs + CI + fallback，不是 business scheduler
```

新版與舊版的關鍵差異已完整寫入主手冊。後續任何 Case0004 操作若仍以 GitHub workflow queue 作 canonical blocker，視為走回舊架構。

## 已完成的執行層遷移

1. `DWG_todo` 已新增純本機 Story Index operator：`scripts/operator_dwg_story_index_local.ps1`。
   - 不呼叫 `gh workflow run`。
   - 直接 build `dwg-editor` / OpenCAD。
   - 直接啟本機 CAD HTTP service。
   - 直接呼叫 `cad.build_story_index` / `cad.render_story_viewports`。
   - receipt 記錄 `OPENWORKER_JOB_ID` / `OPENWORKER_AGENT_SLOT`。
   - commit：`6f72a76a18036e74912c9449241502e7449f63f7`。

2. OpenWorker 已新增 Case0004 本機 durable submitter：`scripts/case0004_submit_story_index_local.ps1`。
   - 查 OpenWorker node / cluster agents。
   - 問 go-tool `/agent/query`。
   - fail-closed 驗證 capability `dwg.story_index.execute.case-worklist`。
   - 使用既有本機 checkout / payload。
   - POST durable job；GitHub 不持有長任務。
   - commit：`491611bd8b3c6c4e7b5efe17bad0ca1cc7a2a87e`。

3. go-tool Story Index business capability 已改為 local-first。
   - GitHub workflow 僅 legacy / emergency。
   - commit：`8ea4226dcf275aeb95a461d5b89b6e1e84802926`。

4. go-tool queue-admin 已改為 OpenWorker durable queue authority 優先。
   - 清隊列為本機即時 operation，不再等另一個 GitHub Action 排隊。
   - commit：`99bed66f22137658f160c8858f1173b2c57a92dd`。

5. Case0004 Worklist 已升 revision 5。
   - `parallel_policy.authority = openworker-local-supervisor`
   - `max_local_slots = 4`
   - `github_business_scheduler = false`
   - `0004-047 = fanout`
   - `0004-049 = join`
   - commit：`cd9a61e8a75f9757913664b957e813ad33bc2abd`。

6. 主手冊已再次更新，把新版／舊版差異、Action 語義、queue/drain、並行模型、Supervisor 操作規則寫清楚。
   - commit：`49198d3c349bccff73d83758633bbd75a8af05a4`

## REAL Case durable 狀態

已存在的 Case evidence 不因 control-plane 升級而失效：

```text
0004-010 PASSED
0004-020 PASSED
0004-030 PASSED
0004-040 PASSED
0004-045 READY
```

Authority inputs：

- DWG SHA256：`aaadbd84e8a5b2e1b0b8f54c16901a69085c7501aeec602929fd994f3192f5b6`
- overview：`D:\AI-Work\jobs\0004-DWG-TO-3D\dwg\exports\default\visual-search\case0004-overview.png`
- overview SHA256：`5cee03340cbbcad51e412b46b85bda9dcaac22b193586b953bbfd5134039103e`
- canonical next step：`0004-045 cad.build_story_index`

禁止重跑 `0004-010 ~ 0004-040`。

## 現在真正要做的工作

`0004-045` 需要從 REAL overview 取得 authoritative visual localization：

- `camera_bounds`
- `image_width` / `image_height`
- 每層 `pixel_bounds`
- confidence
- visual basis

取得後立即：

```text
case0004-story-index-params.json
-> OpenWorker local durable submit
-> cad.build_story_index
-> REAL Story Index artifact
-> 0004-047 per-story fan-out（最多 4 slots）
-> 0004-049 join + multimodal review
```

## 新版驗收觀念

必須分開兩件事：

### Control path 通

OpenWorker 接受 action / job、分配 durable identity，代表本機 control path 已通；不必像舊 GitHub Actions 一樣阻塞對話等待長任務跑完。

### Business step 完成

仍必須看到真實 artifact、SHA256、receipt、Worklist acceptance 才能把 step 標 PASSED。

因此新版可以快速連續派發可並行工作，但不能用 submit 成功冒充成果成功。

## 下一步

1. 查 OpenWorker O87 最新 durable state / slots / queue。
2. 查 go-tool `cad.build_story_index` 最新 capability。
3. 若 stale blocker，OpenWorker 本機一鍵 drain。
4. 取得 overview visual localization。
5. submit `0004-045`。
6. 成果完成後直接 fan-out `0004-047`。

## 狀態

**DOCUMENTATION UPDATED — NEW LOCAL-FIRST DIFFERENCE EXPLICIT — CASE0004 CONTINUES FROM 0004-045**
