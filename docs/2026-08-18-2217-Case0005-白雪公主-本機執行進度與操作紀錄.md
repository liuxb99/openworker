# Case 0005 白雪公主：本機執行進度與操作紀錄

更新时间：2026-08-19 11:30 +08:00

## 1. 案例基本資料

- Case ID：`0005`
- 名称：`Snow White / 白雪公主`
- 固定执行机器：`DESKTOP-ODAQN0D`
- Workspace：`D:\AI-Work\jobs\0005-SNOW-WHITE`
- 当前 Worklist revision：`13`
- 正常业务模式：`localexec`
- 工具权威：`go-tool-runtime :8848`
- 进度权威：`OpenWorker`
- 固定机器权威：`COMPUTERNAME`
- 本機總控要求：`REAL_VERIFIED + OPERATIONAL + 4 claim slots + 4 executor slots`
- GitHub Actions：允许作为 transient command transport；不得成为 Case business execution / queue / artifact authority。
- Google Drive：仅用于成果发布及 ChatGPT 审查，不作为业务命令入口。

## 2. 当前权威进度

依据 `case-worklists/0005.json` revision 13，业务步骤目前仍无新的 acceptance evidence；第一个业务目标仍为：

`0005-010 Generate fresh structured Snow White Director preproduction` → `PENDING`

后续依序为 `0005-020 -> 0005-025 -> 0005-026 -> 0005-027`，第一個用户可见验收点为 text-only storyboard PPTX 发布到 Google Drive。

没有 acceptance evidence 的业务 step 不得宣告完成。

## 3. bootstrap gate

最新 `evidence/case0005/latest-bootstrap-result.json` 已确认：

- `succeeded=true`
- `stage=completed`
- machine=`DESKTOP-ODAQN0D`
- workspace 已创建
- node online=`true`
- node max workers=`4`
- go-tool authority=`C:\ProgramData\go-tool-runtime\work-agent`
- local queue=`:8848`
- bootstrap durable job accepted=`true`
- `github_action_used=false`
- next_action=`continue with OpenWorker local supervisor`

旧 `resolve_tool_roots` blocker 已解除。

## 4. 新控制链

目标链：

`ChatGPT -> command-requests/oda.json -> GitHub transient Action -> ODA openworkerctl -> localhost:8848 -> go-tool durable queue -> OpenWorker local supervisor -> local Action`

边界：GitHub 只允许 `supervisor_status / case_status / case_continue / queue_clear` 等高阶短命令；不得直接跑 Director、OpenMAIC、ComfyX、Blender、影片业务。

## 5. 2026-08-19 实际操作记录

### 5.1 第一轮 case_continue / case_status

已发送：

- `20260819-1100-oda-case0005-continue-001`
- `20260819-1102-oda-case0005-status-001`
- `20260819-1107-oda-case0005-status-002`
- `20260819-1116-oda-case0005-continue-002`

最后一次 `case_continue` Git commit：`29082e033b47a19fb64bb10b05d6839ad7fdcfcc`。

### 5.2 transport readback 验证

截至 2026-08-19 11:30 +08:00：

- `command-requests/oda.json` 已确实存在，最后 request 是 `20260819-1116-oda-case0005-continue-002`。
- Git commit diff 已验证 request 从 `case_status` 正确变成 `case_continue`。
- `command-results/oda.json` 仍为 `404 Not Found`。
- 针对该 request commit 查询 connector 可见 workflow runs，返回空集合；此接口本身只覆盖特定关联 run，不能据此证明 GitHub 完全没有 run，但至少目前没有可读回的真实 transport receipt。
- 因此不能宣告 ODA 已接到第二次 `case_continue`，也不能宣告 `0005-010` 已 submitted/running。

### 5.3 当前发现的真实缺口

现在 Case 0005 的 blocker 已从旧 bootstrap 问题缩小为：

**ChatGPT/GitHub request 已落盘，但 transient transport 的可验证 readback 尚未闭环。**

这不是 Director/ComfyX 业务失败。当前缺少的是以下至少一种真实证据：

1. `command-results/oda.json` transport receipt；或
2. OpenWorker `case_status` 回读出的 work_id/status/evidence；或
3. ODA 本机 ledger 对应 request_id 的 accepted event。

在取得以上证据前，不重复无意义地连续发送 `case_continue`，避免未来 transport 恢复后产生重复执行；应优先修通/验证 idempotent receipt。

## 6. 0005-010 成功标准

Capability：`comfyx-studio.director.preproduction`。

必须取得：

- `run_id`
- `director_plan`
- `director_plan_sha256`
- `shot_count`
- `character_count`
- `scene_bible_count`

之后才能推进 `0005-020`。

## 7. 下一步

1. 查明 push-trigger transient workflow 的实际文件/触发条件与 ODA runner 接单情况。
2. 让 transport receipt 落为可由 ChatGPT connector 读取的 bounded JSON。
3. receipt 必须包含 request_id、machine、case_id、command、accepted/rejected、local job/work id（如有）、`github_action_used_for_command_transport=true`、`github_action_used_for_business_execution=false`。
4. 一旦确认 `20260819-1116-oda-case0005-continue-002` 未被接受，再安全重发；若已接受则只查 `case_status`，不得重复执行。
5. 取得 OpenWorker evidence 后立即同步本文件，再推进下一步。

## 8. 记录原则

- GitHub workflow completed ≠ Case completed。
- Git commit ≠ OpenWorker business evidence。
- submitted/queued/running 必须如实记录。
- 只有 OpenWorker acceptance evidence 完整，step 才能写成功。
- transport 与 business execution 分离。
- 对未知是否已接单的 request，优先查 receipt，不盲目重复提交。
