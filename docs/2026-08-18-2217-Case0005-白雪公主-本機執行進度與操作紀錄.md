# Case 0005 白雪公主：本機執行進度與操作紀錄

更新时间：2026-08-19 11:15 +08:00

## 1. 案例基本資料

- Case ID：`0005`
- 名称：`Snow White / 白雪公主`
- 固定执行机器：`DESKTOP-ODAQN0D`
- Workspace：`D:\AI-Work\jobs\0005-SNOW-WHITE`
- 当前 Worklist revision：`13`
- 正常业务模式：`localexec`
- 工具权威：`go-tool-runtime :8848`
- 进度权威：`OpenWorker`
- 固定机器权威：`COMPUTERNAME`
- 本機總控要求：`REAL_VERIFIED + OPERATIONAL + 4 claim slots + 4 executor slots`
- GitHub Actions：允许作为 transient command transport；不得成为 Case business execution / queue / artifact authority。
- Google Drive：仅用于成果发布及 ChatGPT 审查，不作为业务命令入口。

## 2. 当前权威进度

依据 `case-worklists/0005.json` revision 13：

- `0005-010` Director preproduction：`PENDING`
- `0005-020` storyboard presentation request：`PENDING`
- `0005-025` text-only storyboard PPTX：`PENDING`
- `0005-026` publish text-only storyboard to Drive：`PENDING`
- `0005-027` wait text-only storyboard approval：`PENDING`
- `0005-030` REAL character master images：`PENDING`
- `0005-040` REAL scene concept images：`PENDING`
- `0005-050` REAL shot images bind：`PENDING`
- `0005-055` illustrated storyboard PPTX：`PENDING`
- `0005-056` publish illustrated storyboard to Drive：`PENDING`
- `0005-057` wait illustrated storyboard approval：`PENDING`
- `0005-060` per-shot REAL video fanout：`PENDING`
- `0005-070` final MP4 join + physical QC：`PENDING`
- `0005-080` WorkLedger revision：`PENDING`
- `0005-082` AI-Engineering-OS identity：`PENDING`
- `0005-085` Engineering OS artifact register：`PENDING`
- `0005-090` final review bundle to Drive：`PENDING`
- `0005-100` ChatGPT Drive review receipt：`PENDING`
- `0005-110` approved Delivery Revision：`PENDING`
- `0005-120` final delivery validation：`PENDING`

没有 acceptance evidence 的业务 step 不得宣告完成。

## 3. 已确认 bootstrap gate 已通过

最新 `evidence/case0005/latest-bootstrap-result.json` 已是 `openworker/case0005-bootstrap-script-outcome/v5`：

- `succeeded=true`
- `stage=completed`
- machine=`DESKTOP-ODAQN0D`
- workspace 已创建
- node online=`true`
- node max workers=`4`
- go-tool authority=`C:\ProgramData\go-tool-runtime\work-agent`
- authority kind=`deployed-runtime-exe`
- local queue=`:8848`
- bootstrap durable job accepted=`true`
- `github_action_used=false`
- next_action=`continue with OpenWorker local supervisor`

因此 2026-08-18 23:04 文件中旧的 `resolve_tool_roots` blocker 已解除，不得再把旧失败当当前状态。

## 4. 2026-08-19 新控制链

为了让 ChatGPT Plus 继续沿 GitHub connector 发令、同时避免旧式 GitHub Action 塞车，现采用：

`ChatGPT -> 更新 command-requests/oda.json -> GitHub push-triggered transient Action -> ODA openworkerctl -> localhost:8848 -> go-tool durable queue -> OpenWorker local supervisor -> local Action`

强制边界：

1. GitHub Action 只发固定高阶命令：`supervisor_status / case_status / case_continue / queue_clear`。
2. Action 不执行 Director、OpenMAIC、ComfyX、Blender、影片等 Case workload。
3. `case_continue` 取得本机 accepted receipt 后即退出，不等待业务完成。
4. Case 状态仍以 OpenWorker durable state/evidence 为准。
5. artifact 仍由 ODA 本机直接发布 Google Drive。
6. transport receipt 必须标记 `github_action_used_for_command_transport=true`、`github_action_used_for_business_execution=false`。

## 5. 2026-08-19 11:00～11:15 实际操作记录

### 5.1 已发送 case_continue

- request_id：`20260819-1100-oda-case0005-continue-001`
- command：`case_continue`
- machine：`DESKTOP-ODAQN0D`
- case_id：`0005`
- Git commit：`c3116fb6de18e007d108ef43e00cef425c0f4a71`
- 语义：只要求本机总控继续 Case，不代表业务完成。

### 5.2 已发送 case_status

- request_id：`20260819-1102-oda-case0005-status-001`
- command：`case_status`
- machine：`DESKTOP-ODAQN0D`
- case_id：`0005`
- Git commit：`6f901116777438ccc47f6cbf82e883311ab38c1f`

随后因 transport receipt 尚不能从 workflow summary 稳定读回，新增 `command-results/oda.json` 设计：只回写本机总控 command receipt，不回写业务 artifact，也不替代 OpenWorker authority。

### 5.3 已重新触发 openworkerctl 安装/验证

为排除 ODA 尚未安装最新控制 CLI，已通过安装 trigger 重新要求 ODA 安装/验证 `openworkerctl`。该动作属于 control-plane deployment，不是 Case business execution。

### 5.4 当前 transport 观察

- `command-requests/oda.json`：可以从 ChatGPT/GitHub connector 更新。
- `command-results/oda.json`：截至本次同步尚未出现。
- 因此当前未取得新的 OpenWorker `case_status` readback，不能把 Git push 本身当作 Case 已推进的证据。
- Google Drive 当前也尚未发现 Case 0005 text-only storyboard PPTX。

## 6. 当前第一个业务目标

仍为 `0005-010 Generate fresh structured Snow White Director preproduction`。

Capability：`comfyx-studio.director.preproduction`。

成功 acceptance evidence 必须至少包含：

- `run_id`
- `director_plan`
- `director_plan_sha256`
- `shot_count`
- `character_count`
- `scene_bible_count`

完成后才能进入 `0005-020 -> 0005-025 -> 0005-026`，第一個用户可见验收点为 text-only storyboard PPTX 发布到 Google Drive。

## 7. 下一步

1. 先取得 ODA transient transport 的真实 receipt。
2. 用 `case_status` 读回 OpenWorker 当前 step/work_id/evidence/blocker。
3. 若 `0005-010` 尚未 submitted，则再次 `case_continue`，由本机总控提交，不允许 GitHub Action直接跑 Director。
4. 若本机总控报告 queue blocker，再用 `queue_clear` 处理后继续。
5. 每取得一个新的 OpenWorker evidence / blocker / work_id，都同步追加到本案例文档。
6. `0005-025` 成功后立即推进 `0005-026`，由 ODA 本机直接上传 text-only storyboard PPTX 到 Google Drive供 ChatGPT/用户审查。

## 8. 记录原则

- GitHub workflow completed ≠ Case completed。
- Git commit ≠ OpenWorker business evidence。
- submitted/queued/running 必须如实记录，不得写成功。
- 只有 OpenWorker acceptance evidence 完整，step 才能写成功。
- transport 与 business execution 分离；GitHub 只负责短命投递。
