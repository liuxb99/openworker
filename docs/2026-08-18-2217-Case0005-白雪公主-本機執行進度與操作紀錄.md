# Case 0005 白雪公主：本機執行進度與操作紀錄

更新时间：2026-08-19 11:10 +08:00

## 1. 案例基本資料

- Case ID：`0005`
- 名称：`Snow White / 白雪公主`
- 固定执行机器：`DESKTOP-ODAQN0D`
- Workspace：`D:\AI-Work\jobs\0005-SNOW-WHITE`
- 当前 Worklist revision：`13`
- 正常业务模式：`localexec`
- 工具权威：`go-tool-runtime :8848`
- 进度权威：`OpenWorker`
- 固定机器权威：`COMPUTERNAME`
- 本机总控要求：`REAL_VERIFIED + OPERATIONAL + 4 claim slots + 4 executor slots`
- GitHub Actions：只允许 transient command transport / control-plane install；不得成为 Case business execution / queue / artifact authority。
- Google Drive：只用于成果发布与 ChatGPT/用户审查，不作为业务命令入口。

## 2. 当前业务进度

依据 `case-worklists/0005.json` revision 13，目前尚无新的业务 acceptance evidence。

第一业务步骤仍为：

`0005-010 Generate fresh structured Snow White Director preproduction` → `PENDING`

成功必须取得：

- `run_id`
- `director_plan`
- `director_plan_sha256`
- `shot_count`
- `character_count`
- `scene_bible_count`

完成后才进入：

`0005-020 -> 0005-025 -> 0005-026 -> 0005-027`

第一个用户可见验收点仍是 text-only storyboard PPTX 由 ODA 本机直接发布到 Google Drive。

## 3. Bootstrap gate 已通过

`evidence/case0005/latest-bootstrap-result.json` 已确认：

- `succeeded=true`
- `stage=completed`
- machine=`DESKTOP-ODAQN0D`
- workspace 已创建
- node online=`true`
- node max workers=`4`
- go-tool authority=`C:\ProgramData\go-tool-runtime\work-agent`
- local queue=`:8848`
- bootstrap durable job accepted=`true`
- `github_action_used=false`
- next_action=`continue with OpenWorker local supervisor`

旧 `resolve_tool_roots` blocker 已解除。

## 4. 当前控制链

`ChatGPT -> command-requests/oda.json -> GitHub cloud ingress -> ODA self-hosted dispatch -> openworkerctl -> localhost:8848 -> go-tool durable queue -> OpenWorker local supervisor -> local Action`

强制边界：

1. GitHub 只允许 `supervisor_status / case_status / case_continue / queue_clear`。
2. GitHub 不直接跑 Director、OpenMAIC、ComfyX、Blender、影片业务。
3. `oda_accepted` 只代表本机高阶命令被接受，不代表 Case step 完成。
4. 业务完成只认 OpenWorker acceptance evidence。

## 5. 2026-08-19 transient transport 缺口修复记录

### 5.1 Cloud ingress/readback 已闭环

已完成：

- failure receipt 持久化：`91471dc024251ddfe05a8856172f8be35c7c9a73`
- accepted/rejected 都先写回 Git：`d4445e7e12e83f7a98fa451016f1c8922fe2c38c`
- cloud ingress 先发布 `queued_for_oda`：`a569fd1699cce46531f1e3d1d8c6d106bf334943`
- 只串行 ODA dispatch，不阻塞 ingress：`ffb8f2a7fa4f4c506df48f8faee83d24376d46ee`
- ODA runner registry lookup 加入 receipt，但 GitHub token 对 runner registry API 返回 `403 Forbidden`；该 lookup 已改为非阻断：`976058cf977332110bf336466b88d475795a1161`

因此 `command-results/oda.json` 已不再 404，能明确区分：

- `queued_for_oda`
- `oda_accepted`
- `oda_rejected`

runner registry 的 403 只是附加可观测信息权限不足，不影响 command transport。

### 5.2 找到第一个真实 ODA dispatch 失败根因：ExecutionPolicy

Run：`32210372749`

Job：`dispatch / 95941762738`

真实 runner evidence：

- Runner name：`DESKTOP-ODAQN0D-R001`
- Machine name：`DESKTOP-ODAQN0D`

说明 ODA label 与 COMPUTERNAME 本身没有错。

失败发生在 GitHub inline PowerShell 临时脚本：

`PSSecurityException: running scripts is disabled on this system`

因此原先把它理解为“ODA runner 未接单”不准确；实际上 ODA 已接单，只是 Windows ExecutionPolicy 阻止 GitHub 生成的 temp `.ps1`。

修复：

- 新增固定仓库脚本 `scripts/run-oda-transient-command.ps1`
- workflow 改为 `cmd.exe -> powershell.exe -ExecutionPolicy Bypass -File <repo script>`
- commit：`9e9d12c0133c8a94497d9368b79c556375a47731`

### 5.3 ExecutionPolicy 修复后，成功进入 wrapper，发现第二个真实缺口

Run：`32210822948`

Runner：`DESKTOP-ODAQN0D-R001`

ExecutionPolicy 已成功越过，实际进入 `invoke-local-supervisor-command-transport.ps1`。

真实 rejection receipt：

- command=`case_status`
- machine=`DESKTOP-ODAQN0D`
- accepted=`false`
- exit_code=`127`
- error=`openworkerctl is not installed: C:\ProgramData\OpenWorker\bin\openworkerctl.exe`

因此第二个 blocker 是 ODA 尚未安装 `openworkerctl`，不是 OpenWorker Case 业务失败。

同时修正 `run-oda-transient-command.ps1`：调用另一个 `.ps1` 不再错误依赖 `$LASTEXITCODE`，改为永远解析 wrapper JSON receipt。

Commit：`ef9b048197fcc3e0081a45944e47cf5d26c43aa0`

## 6. ODA openworkerctl 安装缺口修复

### 6.1 Installer 同样存在 ExecutionPolicy 问题

原 `.github/workflows/install-openworkerctl-oda.yml` 使用 inline PowerShell，因此也会受到同一 Windows ExecutionPolicy 影响。

已改为：

`cmd.exe -> powershell.exe -ExecutionPolicy Bypass -File scripts/install-openworkerctl-oda-action.ps1`

主要 commits：

- `bbb83e49d67efe9c01d3a9addc64574d4de79927`
- `272f514aeee1c21712838654dc9c23f8e5eee12c`

### 6.2 第一轮 installer 回执发现 PowerShell quoting bug

Install run：`32210994754`

`command-results/oda-install.json` 成功落回 Git，但状态：

- `succeeded=false`
- machine=`DESKTOP-ODAQN0D`
- runner=`DESKTOP-ODAQN0D-R001`

真实错误位于 `scripts/install-openworkerctl.ps1`：

CMD shim 字符串使用了 Bash/C 风格 `\"`，在 PowerShell 中造成 parser error。

已改为 PowerShell 正确反引号转义：

`$shimText="@echo off`r`n`"$target`" %*`r`n"`

Commit：`68e717e1e100f747753632951b8c2307966dabe2`

### 6.3 Installer 也改为两阶段可观测

为了不再猜安装 job 是否接单，installer 现在：

1. GitHub-hosted ingress 先写 `QUEUED_FOR_ODA` + `github_run_id`。
2. ODA job 完成后覆盖为 `REAL_VERIFIED` 或 `FAILED`。

Commit：`337dc3d493d60c47330257c6b755381acc33a082`

### 6.4 ODA openworkerctl 已 REAL_VERIFIED

最新 `command-results/oda-install.json`：

- schema=`openworkerctl-oda-install/v3`
- `succeeded=true`
- status=`REAL_VERIFIED`
- machine=`DESKTOP-ODAQN0D`
- runner=`DESKTOP-ODAQN0D-R001`
- executable=`C:\ProgramData\OpenWorker\bin\openworkerctl.exe`
- supervisor_status=`OPERATIONAL`
- route_label=`LOCAL_SUPERVISOR`
- github_run_id=`32211043838`
- `github_action_used_for_business_execution=false`
- observed_at=`2026-08-19T03:08:19Z`（11:08:19 +08:00）

**结论：OpenWorker control-plane 安装与 localhost:8848 supervisor 验证已真实成功。**

## 7. 最新 Case status 查询

安装成功后已发出新的只读 `case_status`。

`status-010`：

- request commit：`3e98681479bac3ed9463aaafd28229e0887e7197`
- GitHub run：`32211118354`
- ingress：`completed / success`
- dispatch job：`queued`

这次 queued 的含义已经与之前不同：

- ExecutionPolicy 已修复。
- openworkerctl 已安装并 REAL_VERIFIED。
- localhost supervisor 已验证 `OPERATIONAL / LOCAL_SUPERVISOR`。
- 当前只是 self-hosted ODA runner 正在处理其他 GitHub job，`case_status` dispatch 尚未取得 runner slot。

已另外发出只读 `status-011`；由于它只是 status 查询，不会产生 Case 业务重复执行。但后续不再继续堆 status request，优先让现有 dispatch 出队。

注意：部分 request_id 中的时分数字只是 client-generated id，不作为权威时间；权威时间以 GitHub run / receipt 的 `observed_at` 为准。

## 8. 当前唯一下一步

1. 继续追 `32211118354` 的 dispatch job。
2. 一旦 dispatch 从 `queued` 进入 terminal：
   - 若 `oda_accepted`：读取 OpenWorker `case_status.result` 的 step/work_id/evidence/blocker。
   - 若 `oda_rejected`：按 receipt 的 exit_code/error 修真实本机缺口。
3. 若 `0005-010` 仍 PENDING 且没有 existing work_id，才发新的 `case_continue`。
4. 若 `0005-010` 已 QUEUED/RUNNING，则只追状态，不重复提交。
5. `0005-010` acceptance evidence 完整后立即推进 `0005-020 -> 0005-025 -> 0005-026`。
6. 每取得真实 receipt / work_id / evidence / blocker 都同步更新本文件。

## 9. 记录原则

- GitHub workflow completed ≠ Case completed。
- Git commit ≠ OpenWorker business evidence。
- `queued_for_oda` ≠ ODA 已执行 command。
- `oda_accepted` ≠ Case business step 完成。
- submitted/queued/running 必须如实记录。
- 只有 OpenWorker acceptance evidence 完整，step 才能写成功。
- 同一个 request_id 必须幂等。
- failure 必须先留下可读 receipt 再 surface workflow failure。
- GitHub 只做 transient transport；Case 业务仍由 ODA local supervisor 执行。
