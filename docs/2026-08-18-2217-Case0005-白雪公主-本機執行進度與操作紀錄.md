# Case 0005 白雪公主：本機執行進度與操作紀錄

更新时间：2026-08-19 12:05 +08:00

## 1. 案例基本資料

- Case ID：`0005`
- 名称：`Snow White / 白雪公主`
- 固定执行机器：`DESKTOP-ODAQN0D`
- Workspace：`D:\AI-Work\jobs\0005-SNOW-WHITE`
- 当前 Worklist revision：`13`
- 正常业务模式：`localexec`
- 工具权威：`go-tool-runtime :8848`
- 进度权威：`OpenWorker`
- 固定机器权威：`COMPUTERNAME`
- 本機總控要求：`REAL_VERIFIED + OPERATIONAL + 4 claim slots + 4 executor slots`
- GitHub Actions：允许作为 transient command transport；不得成为 Case business execution / queue / artifact authority。
- Google Drive：仅用于成果发布及 ChatGPT 审查，不作为业务命令入口。

## 2. 当前权威进度

依据 `case-worklists/0005.json` revision 13，业务步骤目前仍无新的 acceptance evidence；第一个业务目标仍为：

`0005-010 Generate fresh structured Snow White Director preproduction` → `PENDING`

后续依序为 `0005-020 -> 0005-025 -> 0005-026 -> 0005-027`，第一個用户可见验收点为 text-only storyboard PPTX 发布到 Google Drive。

没有 acceptance evidence 的业务 step 不得宣告完成。

## 3. bootstrap gate

最新 `evidence/case0005/latest-bootstrap-result.json` 已确认：

- `succeeded=true`
- `stage=completed`
- machine=`DESKTOP-ODAQN0D`
- workspace 已创建
- node online=`true`
- node max workers=`4`
- go-tool authority=`C:\ProgramData\go-tool-runtime\work-agent`
- local queue=`:8848`
- bootstrap durable job accepted=`true`
- `github_action_used=false`
- next_action=`continue with OpenWorker local supervisor`

旧 `resolve_tool_roots` blocker 已解除。

## 4. 控制链

目标链：

`ChatGPT -> command-requests/oda.json -> GitHub cloud ingress -> ODA self-hosted dispatch -> openworkerctl -> localhost:8848 -> go-tool durable queue -> OpenWorker local supervisor -> local Action`

边界：GitHub 只允许 `supervisor_status / case_status / case_continue / queue_clear` 等高阶短命令；不得直接跑 Director、OpenMAIC、ComfyX、Blender、影片业务。

## 5. 2026-08-19 实际操作记录

### 5.1 已发送请求

- `20260819-1100-oda-case0005-continue-001`
- `20260819-1102-oda-case0005-status-001`
- `20260819-1107-oda-case0005-status-002`
- `20260819-1116-oda-case0005-continue-002`
- `20260819-1138-oda-case0005-status-003`
- `20260819-1146-oda-case0005-status-004`
- `20260819-1152-oda-case0005-status-005`
- `20260819-1200-oda-case0005-status-006`

最新 request commit：`cd797194865e84d158452ef658088ca539b46a12`。

### 5.2 readback 缺口修复

仓库原本已有 `.github/workflows/dispatch-local-supervisor-command-oda.yml`，但旧实现有两类可观测性问题：

1. 本机 openworkerctl 失败时 wrapper 先 throw，导致失败 receipt 永远写不回 Git。
2. concurrency 最初使用每次不同的 run_id，无法真正串行；后来改成 workflow 顶层固定 concurrency 后，又会出现前一个 ODA job 卡住时，后续连 cloud ingress 都无法先落 `queued` receipt。

### 5.3 已完成的修复批次

#### A. transport wrapper：失败也先生成 receipt

Commit：`91471dc024251ddfe05a8856172f8be35c7c9a73`

- 所有 bounded command 统一输出 `openworker.command-transport.v1`。
- openworkerctl 不存在、非 JSON、exit != 0、case_continue 未 accepted，都先生成 receipt。
- receipt 包含 `accepted / exit_code / error / result`。
- 明确 `github_action_used_for_business_execution=false`。
- request_id 本机持久化至 `%ProgramData%\OpenWorker\command-transport\receipts`，workflow rerun 不重复业务命令。

#### B. workflow：失败 receipt 先发布，再让 workflow fail

Commit：`d4445e7e12e83f7a98fa451016f1c8922fe2c38c`

- accepted/rejected 都写 `command-results/oda.json`。
- 只有 receipt 已经可读后才 surface workflow failure。

#### C. cloud ingress 分阶段可观测

Commit：`a569fd1699cce46531f1e3d1d8c6d106bf334943`

增加 GitHub-hosted `ubuntu-latest` ingress job：

- 先验证 request schema / machine / case / command。
- 在等待 ODA runner 之前先发布 `phase=queued_for_oda`。
- ODA 真正执行后再覆盖为 `phase=oda_accepted` 或 `phase=oda_rejected`。

#### D. 只串行 ODA dispatch，不阻塞 cloud ingress

Commit：`ffb8f2a7fa4f4c506df48f8faee83d24376d46ee`

- 移除 workflow 顶层 concurrency。
- concurrency 下移到 `dispatch` job：`openworker-command-transport-oda-dispatch`。
- 因此即使 ODA runner 长时间没领取，新的 cloud ingress 仍能落可见状态；真正本机 dispatch 仍保持串行。

### 5.4 已取得第一个真实 staged receipt

`command-results/oda.json` 已不再 404。

真实内容：

- schema=`openworker.command-result.v1`
- phase=`queued_for_oda`
- request_id=`20260819-1152-oda-case0005-status-005`
- command=`case_status`
- machine=`DESKTOP-ODAQN0D`
- github_run_id=`32210300174`
- `github_action_used_for_command_transport=true`
- `github_action_used_for_business_execution=false`
- `business_completion_claimed=false`
- note=`GitHub ingress validated; waiting for ODA self-hosted runner. This is not local acceptance.`

这证明：

1. ChatGPT -> GitHub request 已打通。
2. GitHub workflow trigger 已打通。
3. cloud ingress 已打通。
4. `command-results` readback 已打通。
5. 当前真正剩余 blocker 已缩小为：**ODA self-hosted runner 尚未把该 dispatch job推进到本机 openworkerctl acceptance**。

因此目前不能把 `0005-010` 写成 submitted/running，也不能重发 case_continue；应先等到 `oda_accepted/oda_rejected` 或修 ODA runner pickup。

## 6. 0005-010 成功标准

Capability：`comfyx-studio.director.preproduction`。

必须取得：

- `run_id`
- `director_plan`
- `director_plan_sha256`
- `shot_count`
- `character_count`
- `scene_bible_count`

之后才能推进 `0005-020`。

## 7. 下一步

1. 读取下一版 `command-results/oda.json`。
2. 若 phase=`oda_accepted`：读取 OpenWorker case status，确认当前 step/work_id/evidence/blocker，再安全决定是否发 `case_continue`。
3. 若 phase=`oda_rejected`：按 receipt 的 `exit_code/error` 修本机真实缺口。
4. 若长期停在 `queued_for_oda`：只修 ODA self-hosted runner pickup / runner service，不再改 OpenWorker business code。
5. 每取得新 receipt / evidence 都同步更新本文件。

## 8. 记录原则

- GitHub workflow completed ≠ Case completed。
- Git commit ≠ OpenWorker business evidence。
- `queued_for_oda` ≠ ODA 已接单。
- `oda_accepted` 仅代表本机高阶命令被接受，不代表 Case business step 完成。
- submitted/queued/running 必须如实记录。
- 只有 OpenWorker acceptance evidence 完整，step 才能写成功。
- 同一个 request_id 必须幂等。
- 失败也必须先留下可读 receipt，再让 workflow fail。
