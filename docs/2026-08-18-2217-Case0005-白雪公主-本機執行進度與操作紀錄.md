# Case 0005 白雪公主：本機執行進度與操作紀錄

更新时间：2026-08-18 22:17 +08:00

## 1. 案例基本資料

- Case ID：`0005`
- 名称：`Snow White / 白雪公主`
- 固定执行机器：`DESKTOP-ODAQN0D`
- Workspace：`D:\AI-Work\jobs\0005-SNOW-WHITE`
- 当前 Worklist revision：`4`
- 正常业务模式：`localexec`
- 工具权威：`go-tool-runtime`
- 进度权威：`OpenWorker`
- 影片规划权威：`ComfyX durable knowledge graph`
- 固定机器权威：`COMPUTERNAME`
- GitHub Actions：仅允许安装、升级、修复、recovery / code update；不得执行 Case 业务。
- Google Drive：仅用于成果发布及 ChatGPT 审查，不作为命令入口。

## 2. 当前权威进度

依据 `case-worklists/0005.json` revision 4，OpenWorker 当前记录为：

- `0005-010` Generate fresh structured Snow White Director preproduction：`PENDING`
- `0005-020` Materialize storyboard presentation request：`PENDING`
- `0005-025` Generate text-only storyboard PPTX：`PENDING`
- `0005-027` User approval of text-only storyboard：`PENDING`
- `0005-030` REAL character master images：`PENDING`
- `0005-040` REAL scene concept images：`PENDING`
- `0005-050` Generate and bind REAL shot images：`PENDING`
- `0005-055` Generate illustrated storyboard PPTX：`PENDING`
- `0005-057` User approval of illustrated storyboard：`PENDING`
- `0005-060` Fan out approved shots as REAL video durable jobs：`PENDING`
- `0005-070` Assemble canonical final MP4 + physical QC：`PENDING`
- `0005-080` Artifact registry + WorkLedger revision：`PENDING`
- `0005-090` Publish review bundle to Google Drive：`PENDING`
- `0005-100` Apply ChatGPT review receipt：`PENDING`
- `0005-110` Create approved Delivery Revision：`PENDING`
- `0005-120` Validate final delivery package：`PENDING`

当前所有 step 的 `evidence={}`、`blocker=""`。因此案例尚未取得任何可宣告完成的业务步骤，不能从旧 trigger、GitHub commit 或部署纪录推定案例已有进展。

## 3. 第一個必须执行的业务步骤

### Step `0005-010`

标题：`Generate fresh structured Snow White Director preproduction`

允许 action：

- `comfyx-studio.director.preproduction`

依赖：无。

完成必须取得以下 acceptance evidence：

- `run_id`
- `director_plan`
- `director_plan_sha256`
- `shot_count`
- `character_count`
- `scene_bible_count`

只有以上 evidence 实际回写 OpenWorker 后，才允许把 `0005-010` 从 `PENDING` 改为成功终态，再进入 `0005-020`。

## 4. 正确执行链

业务链固定为：

`ChatGPT / llama.cpp Coder`
→ `go-tool`
→ `DESKTOP-ODAQN0D:8848`
→ `durable queue`
→ `work-agent`
→ `executor`
→ `OpenWorker local supervisor`
→ 对应业务 action
→ 回写 OpenWorker worklist / evidence
→ 成果文件进入 Case workspace

不得改成：

- Google Drive OWCMD/OWACK ingress
- GitHub Actions 业务调度
- 仅凭 GitHub commit 猜案例状态
- 模型自行猜 tool/action 名称

## 5. 每次继续案例的查询规则

### 5.1 先问 OpenWorker 进度

每次开始新一轮操作，先读取 OpenWorker 的 Case worklist / runtime status，确认：

1. 当前 revision。
2. 当前第一个非成功终态 step。
3. status。
4. evidence。
5. blocker。
6. work_id / durable job id（若存在）。
7. assigned host。
8. workspace root。

若 OpenWorker 与其他来源冲突，以 OpenWorker 的 durable state / evidence 为准。

### 5.2 再问 go-tool 方法

不要直接猜下一步如何执行。先向 `go-tool-runtime` 查询工具能力和项目 Context。

基本查询顺序：

1. `GET /tools`：确认工具是否在线、有哪些可用工具。
2. `POST /context`：以 `project + task + query` 查询当前任务的 ContextPack。
3. 必要时 `POST /search`：查精确 capability、action、代码路径、历史错误与负面知识。

Case 0005 第一轮建议查询语义：

- project：`ComfyX` / `ComfyX-Studio`（依 go-tool 实际注册项目名为准）
- task：`Case 0005 Snow White director preproduction`
- query：`comfyx-studio.director.preproduction Snow White structured director plan localexec ODA workspace`

go-tool 返回的方法必须优先于模型记忆；若返回 known_errors / warnings / negative knowledge，应记录并遵守，避免重复失败。

## 6. Case 0005 流程与并行规则

OpenWorker revision 4 定义：

- 本机 supervisor 最大 local slots：`4`
- transport：non-blocking
- `0005-027` 用户批准文字分镜后：
  - `0005-030` 角色 master image
  - `0005-040` 场景 concept image
  两支并行，完成后 join 到 `0005-050`。
- `0005-057` 用户批准图像分镜后：
  - 每个 approved shot 建立一个独立 REAL video durable job
  - 全部 shot 成功后 join 到 `0005-070`。

因此影片阶段不是单一大任务，而是 shot-level durable fan-out + join。

## 7. 影片生成阶段约束

进入 `0005-060` 后：

- action：`comfyx.production.video.real`
- 每个 approved shot 必须独立 durable job。
- 必须保留 `shot_job_ids`、`shot_video_receipts`、`shot_videos`、`shot_video_sha256`、`execution_ledger`。
- 必须确认 `all_shots_terminal_succeeded=true` 才可进入 final join。

本案例后续视频策略：

1. MiniMax H3 中解析度快速生成。
2. 以 ComfyX durable knowledge graph 查询当前最合适的 H3 生成方式及加速参数。
3. 通过官方超分流程。
4. 输出高解析度最终 MP4。
5. `0005-070` 做实体 decode / resolution / duration / subtitle / physical QC。

不得在尚未完成两轮 storyboard approval 前提前生成昂贵影片。

## 8. 每一步必须记录的操作日志格式

每一个实际动作完成后，本文件必须追加一条记录，至少包含：

- 时间（Asia/Taipei）
- Case ID / step_id
- 当前 worklist revision
- assigned_host / COMPUTERNAME
- workspace_root
- 进度来源：OpenWorker
- 方法来源：go-tool
- go-tool 查询内容
- go-tool 返回的推荐 capability/action
- 实际提交 action
- 输入参数摘要
- work_id / run_id / durable job id
- 开始状态
- 终态
- OpenWorker evidence 完整字段
- 产物绝对路径与相对路径
- SHA256
- QC / reopen / physical validation
- blocker / error
- 是否产生 negative knowledge
- 修复动作
- 下一步

没有 evidence 的步骤不得写成完成；只有进程启动或任务入队时，应明确写 `SUBMITTED/QUEUED/RUNNING`，不得写成功。

## 9. 2026-08-18 22:17 检查纪录

### 9.1 OpenWorker 进度检查

- 读取：`case-worklists/0005.json`
- revision：`4`
- assigned_host：`DESKTOP-ODAQN0D`
- workspace：`D:\AI-Work\jobs\0005-SNOW-WHITE`
- 第一个可执行 step：`0005-010`
- status：`PENDING`
- evidence：空
- blocker：空
- 结论：尚未开始正式 Case 业务链。

### 9.2 go-tool 方法检查

确认 go-tool-runtime 对 Agent 提供统一 Context 查询：

- `GET /tools`
- `POST /search`
- `POST /context`

其职责是先自动发现可用 MCP/tools，再输出 ContextPack；Agent 不应自行散查或猜 action。

### 9.3 当前下一步

对 ODA 本机 runtime：

1. 先查 `/tools` 健康状态。
2. 用 `/context` 查询 Case 0005 的 `comfyx-studio.director.preproduction` 正确调用方法、输入 schema、负面知识与依赖。
3. 依据 go-tool 回答，将 `0005-010` 作为 durable local work 提交到 `DESKTOP-ODAQN0D:8848`。
4. 由 OpenWorker 查询实际 work_id / 状态。
5. 完成后把 `run_id + director_plan + SHA256 + shot_count + character_count + scene_bible_count` 回写 worklist evidence。
6. 本文件追加完整实际执行记录。

这套顺序为 Case 0005 后续每一步的固定操作规范。
