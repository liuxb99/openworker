# Case 0005：單一 Go 主控 REAL 遷移增量紀錄

更新時間：2026-08-19 12:31 +08:00

## 已確認 REAL evidence

### 1. 單一 Go 控制執行檔已在 ODA REAL_VERIFIED

R10 GitHub run：`32214885422`

`command-results/oda-install/32214885422/final.json` 已確認：

- `succeeded=true`
- `status=REAL_VERIFIED`
- machine=`DESKTOP-ODAQN0D`
- canonical executable=`C:\ProgramData\OpenWorker\bin\openworker.exe`
- compatibility executable=`C:\ProgramData\OpenWorker\bin\openworkerctl.exe`
- supervisor_status=`OPERATIONAL`
- route_label=`LOCAL_SUPERVISOR`
- `single_go_control_authority=true`
- `python_required_for_case_bootstrap=false`
- `github_action_used_for_business_execution=false`

因此 control-plane canonical authority 已正式從分散 CLI/Python controller 收斂到 `openworker.exe`。

### 2. Case 0005 worklist 已升 revision 14

Commit：`32b5f9554635c4b697991f330b1af084fad89b75`

變更：

- 移除 `canonical_controller_module=coworker.case0005_verified_local_controller`
- `canonical_controller=openworker-go-case-engine/v1`
- `python_controller_allowed=false`

第一個 ready business step 仍為 `0005-010`。

### 3. Go-native bootstrap 已在 ODA 真實成功

Request：`20260819-1218-oda-case0005-bootstrap-001`
GitHub run：`32215367661`

雖然舊 transport wrapper 最外層因只檢查頂層欄位而誤標 `oda_rejected / exit 73`，nested authoritative result 明確為成功：

- `controller=go-native`
- `operation=bootstrap`
- `status=completed`
- `python_controller_used=false`
- `python_required=false`
- OpenWorker HTTP status=`202`
- `controller_result.ok=true`
- `controller_result.stage=go_native_bootstrap_completed`
- revision=`14`
- `ready_step_ids=[0005-010]`
- workspace=`D:\AI-Work\jobs\0005-SNOW-WHITE`
- ledger=`D:\AI-Work\jobs\0005-SNOW-WHITE\.openworker\case-supervisor-ledger.jsonl`
- controller snapshot=`D:\AI-Work\jobs\0005-SNOW-WHITE\.openworker\case-controller-last.json`
- worklist snapshot=`D:\AI-Work\jobs\0005-SNOW-WHITE\.openworker\case-worklist.json`
- spec snapshot=`D:\AI-Work\jobs\0005-SNOW-WHITE\.openworker\case-spec.json`

Transport false-negative 已於 commit `d2027ab2316bc7d39bb966a080d4cfe9eb4ac2d0` 修正，改為遞迴辨識 nested Go-native bootstrap completion evidence。

第二次 bootstrap request `20260819-1222-oda-case0005-bootstrap-002` 亦再次得到相同 nested successful Go-native bootstrap evidence；外層仍因該 run 使用修復前 wrapper 而標示 exit 73。它只再次證明 bootstrap 是 idempotent，**不需要再重做 bootstrap**。

## 4. 第一個 Go-native `case_continue` 已真正抵達業務 capability 邊界

唯一 continue request：

- request_id=`20260819-1225-oda-case0005-continue-go-001`
- request commit=`090dba9cc98d3a51631a41f29e3b3abc1122cac5`
- GitHub run=`32215665580`
- runner=`DESKTOP-ODAQN0D-R001`

結果：`oda_rejected`，但這不是 Go controller 或 transport 失敗。

實際執行鏈已成功走到：

`openworker.exe -> go-tool :8848 -> OpenWorker Go Case Engine -> /v1/cases/continue -> :8848 local-work preflight`

Go Case Engine 已選中：

- step=`0005-010`
- capability=`comfyx-studio.director.preproduction`
- execution_route=`local_supervisor`
- `python_controller_used=false`
- `github_action_used_for_business_execution=false`

真正拒絕原因：

`capability is not registered by the installed local executor contract`

當時已註冊 capability 僅包括：

- `comfyx.production.video.real`
- `go-tool.local-supervisor.smoke`
- `go-tool.runtime.update-local`
- `go-tool.supervisor.probe`
- `image.comfyx.storyboard-real`
- `presentation.openmaic`

因此 **0005-010 尚未建立 business work_id，也尚未提交**；不得重複發第二個 continue，必須先補本機 capability contract。

## 5. 找到 capability 未註冊的根因並已修源碼

`internal/localexec/registry.go` 原本已經有 `comfyx-studio.director.preproduction` handler；真正問題在 `scripts/windows/install-gtr-local-work-runtime.ps1` 的 root discovery 已過時：

- Comfyx-Studio 是 Go repo，但 installer 還用 `package.json` 作 marker。
- OpenWorker 已 Go-native，但 installer 還用舊 Python `coworker/case_controller.py` 作 marker。

Comfyx-Studio 權威 repo 已確認語言為 Go，根目錄存在 `go.mod`。

修復 commit：`46be166d871ef51d06e2c56ade6555ab15cadf1d`

新 marker：

- OpenWorker：`go-runtime\go.mod` + `case-worklists\0005.json`
- Comfyx-Studio：`go.mod`

因此新安裝後 registry 應能取得 `ComfyXStudioRoot`，並註冊：

- `comfyx-studio.director.preproduction`
- `comfyx-studio.storyboard.plan`
- `comfyx-studio.storyboard.bind`
- `comfyx-studio.finalize`
- 與 ComfyX image handler 同時存在時的 `comfyx-studio.storyboard.real-bind`

修復已自動觸發 convergence deployment run：`32215911127`。

## 6. go-tool 4-slot runtime 仍需同時驗證

前一輪 convergence deploy `32214594935`：

- targeted tests：成功
- tools build：成功
- install：成功
- durable local queue bootstrap：成功
- persistent control plane health：成功
- 最後 `REAL four-slot local supervisor`：失敗

ODA 因 ScheduledTask 權限 `Access is denied` 退到 `detached-wmi-process`，其後曾出現：

`slot 2: runner heartbeat: Post http://127.0.0.1:8848/api/information/runners/heartbeat: EOF`

新 deployment `32215911127` 必須同時驗證：

1. ODA root discovery 不再警告 OpenWorker / Comfyx-Studio missing。
2. `comfyx-studio.director.preproduction` 出現在 installed executor contract。
3. 4 claim workers + 4 executor slots 取得 `REAL_VERIFIED`。

只有以上完成後，才重發一個新的 `case_continue`；舊 request `...continue-go-001` 已 terminal rejected，不會重用。
