# Case 0005：0005-010 Pending 與 Upgrade V3 六槽擁塞實證

更新時間：2026-08-19 13:10 +08:00

## 1. Case 業務狀態

目前禁止再次提交 `case_continue`。

已接受的唯一目前 business work：

- Case：`0005`
- step：`0005-010`
- capability：`comfyx-studio.director.preproduction`
- revision：`14`
- work_id：`case0005-0005-010-r000014-17b8b780`
- machine：`DESKTOP-ODAQN0D`
- authority：`go-tool-runtime :8848 durable local-work`
- controller：`openworker-go-case-engine/v1`
- Python controller：禁止 / 未使用

最新只讀 Case status 已確認此 work 仍為 `pending`；尚無 `running`、`failed` 或 Director terminal evidence。因此目前不得進入 `0005-020`。

0005-010 成功仍必須取得：

- `run_id`
- `director_plan`
- `director_plan_sha256`
- `shot_count`
- `character_count`
- `scene_bible_count`

## 2. Pending 不等於 Case Engine 失敗

Go-native continue 已經成功完成 submit，Director capability 亦已通過 submission preflight。因此目前問題位於 durable local-work 被 submit 之後、完成 claim/execution 之前。

已新增純只讀 work probe：

- workflow：`.github/workflows/probe-case0005-work-oda.yml`
- workflow commit：`b05f43d5ce31a0fa47afc65b6272345cf4653847`
- trigger：`diagnostic-requests/case0005-work.json`
- trigger commit：`e84e81f85663e900d430d02e72e707d4bea174aa`

該 probe 僅讀：

- `GET /api/execution/local-work/<work_id>`
- `GET /api/execution/local-work/<work_id>/events`
- `GET /api/execution/local-supervisor/status`

不 clear queue、不 retry、不 resubmit business work。

## 3. Upgrade OpenWorker Nodes V3 六槽調度缺口已被 REAL evidence 證實

workflow：`.github/workflows/upgrade-openworker-nodes-v3.yml`

目前使用：

```yaml
strategy:
  fail-fast: false
  max-parallel: 3
  matrix:
    slot: [1, 2, 3, 4, 5, 6]
runs-on: [self-hosted, Windows, X64]
```

也就是六個 generic Windows jobs 搶三台實體機，再於 job 內用 `COMPUTERNAME` 判定 ODA/O87/UL7。

### REAL run

Run：`32217998038`
Target commit：`8aa368b3cf2877598a6c96a46dc208d4d789fc52`

GitHub jobs 真實顯示六個 upgrade jobs 同時存在：

- upgrade (1)
- upgrade (2)
- upgrade (3)
- upgrade (4)
- upgrade (5)
- upgrade (6)

其中多個 job 同時／先後落到同一台實體機。

ODA receipt 亦出現同一 target commit 被後續 slot 覆寫：

- 先為 slot `2`
- 後被 slot `4` 更新

commit：`3834c0c0a24c902b6382097fdff142c1a66c1cc5`

這證明 per-machine file lock 只能避免「同一瞬間」兩個 slot 同時安裝；不能避免前一 slot 釋放 lock 後，另一 generic slot 再次佔用同一機器、執行 setup-go / receipt / verify / post cleanup。

ODA slot 4 的 REAL log 亦顯示：

- runner：`DESKTOP-ODAQN0D-R001`
- route：ODA / slot 4
- setup-go 正常執行
- upgrade verify 執行
- post setup-go cache cleanup 延續超過一分鐘
- 並出現另一 job 同時建立相同 Go cache 的競爭訊息

因此這是已證實的 **GitHub/self-hosted runner congestion + duplicate verification** 缺口。

## 4. 與 0005-010 pending 的關係

目前只能確定：

- Upgrade V3 六槽設計會真實佔用 ODA self-hosted runner，延遲 Case status/work probe 與 command transport。
- 尚不能只憑此證據宣稱它就是 `:8848` resident gtr-work-agent 未 claim 0005-010 的唯一原因；gtr-work-agent 是本機 resident process，仍需 read-only work events / supervisor health 才能判定。

因此修 Upgrade V3 是必要的基礎設施修復，但不能用它取代 durable local-work 根因診斷。

## 5. 下一批修復決策

Upgrade V3 改為一台真實機器一個固定 job：

- ODA：`runs-on: [self-hosted, Windows, X64, ODA]`
- O87：`runs-on: [self-hosted, Windows, X64, O87]`
- UL7：`runs-on: [self-hosted, Windows, X64, UL7]`

不再使用 6 個 generic slot，不再 runtime 搶機後才判斷 `COMPUTERNAME`。

仍保留 COMPUTERNAME fail-closed 驗證，確保 label 沒有誤路由。

驗收：

1. 一次 Upgrade V3 run 僅產生三個 upgrade jobs。
2. ODA 只有一個 job，receipt 不再 slot 2 → slot 4 覆寫。
3. ODA/O87/UL7 各自 running_commit / target_commit / upgrade_verified 正確。
4. Case 0005 business queue 不做 clear、不做 resubmit。
5. ODA runner 釋放後立即讀 `case0005-0005-010-r000014-17b8b780` 的 work/events/supervisor，判斷 submitted→claimed→execution_started 的真正停點。
