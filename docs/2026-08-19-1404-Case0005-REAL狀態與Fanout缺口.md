# Case 0005：REAL 狀態與 030/040 Fanout 缺口

更新時間：2026-08-19 14:04 +08:00

## 1. REAL 業務 authority

目前唯一已接受的 Case 0005 durable business work 保持不變：

- step：`0005-010`
- capability：`comfyx-studio.director.preproduction`
- work_id：`case0005-0005-010-r000014-17b8b780`
- revision：14
- queue authority：`http://127.0.0.1:8848`

本輪沒有 clear queue、沒有 retry 0005-010、沒有建立第二個 business work。

## 2. 新只讀 status request

已提交：

- request_id：`20260819-1404-oda-case0005-status-go-009`
- command：`case_status`
- machine：`DESKTOP-ODAQN0D`
- case_id：`0005`
- request commit：`743d30a4dbc665d4c9d88812f2a10c4199226bb2`

目的：由 canonical `openworker.exe case status 0005` 直接返回 workspace authority 與 durable `current_work`。

截至本文件更新時：

- `command-results/oda/20260819-1404-oda-case0005-status-go-009/queued.json` 尚未出現；
- `final.json` 尚未出現；
- `.github/workflows/dispatch-local-supervisor-command-oda.yml` 仍正確監聽 `command-requests/oda.json`；
- 因此目前判定為 GitHub ingress 未落 receipt，不得把它誤判成 ODA business failure。

## 3. Source 主鏈已完成到 text-only approval gate

目前 Go-native SOURCE contract：

`0005-010 Director`
→ `0005-020 storyboard request`
→ `0005-025 text-only storyboard PPTX`
→ `0005-026 ODA direct Google Drive publish`
→ `0005-027 APPROVE/REJECT gate`

最新相關 commits：

- terminal reconciliation：`52b8e70ece18823e9c01fed523d5ef539c288b97`
- 025 mapping：`181b1ad0ebd047ed123f21e456512f8e50e7b149`
- 026 mapping：`25aef4642364374b2a6c8e446d54ef99cecefd64`
- 027 gate mapping：`6166f18c4f798425778d654a96d2bf5b086a1e36`
- Drive publish evidence aliases：go-tool-runtime `ec8fbaa7ca053b768a20ad8add18d8b994a8261e`

## 4. 新確認的 030/040 Fanout 缺口

`0005-027` APPROVE 後，worklist 會同時讓兩個步驟 READY：

- `0005-030` character master images
- `0005-040` scene concept images

現有 `Continue()` 在 `len(ready) > 1` 時仍 fail-closed：

`multiple ready steps require queue-owned fanout coordinator`

因此 Go Case Engine 尚不能自然跨過 027。

## 5. 正確 Fanout contract

不能把 030/040 各自包成單一 long-running child，也不能交給 GitHub Action 做 business fanout。

正確行為：

1. 讀 canonical `visual-assets/requirements.json`；
2. 依 role 分出 `character_master` 與 `scene_concept`；
3. 每個 asset 各自產生 deterministic child work_id；
4. 所有 children 直接 POST 到同一 `:8848/api/execution/local-work`；
5. capability 固定 `image.comfyx.storyboard-real`；
6. input 只允許 `workspace_root / assigned_host / asset_id / requirements_relpath`；
7. 先 submit 全部 children，再由一個 Go coordinator 做 terminal join；
8. 030 acceptance 聚合：`character_receipts / character_images / character_sha256`；
9. 040 acceptance 聚合：`scene_receipts / scene_images / scene_sha256`；
10. 任一 child failed → 對應 parent step FAILED，禁止 0005-050；
11. 全部 children succeeded → 030/040 同時 SUCCEEDED，下一 ready step 才是 0005-050。

## 6. 強制邊界

- Python Case controller：禁止。
- GitHub Action business execution：禁止。
- GitHub artifact transport：禁止。
- queue owner：只有 go-tool-runtime :8848。
- 0005-010 在 terminal authority 尚未讀回前不得重提。
- 0005-027 未 APPROVE 前不得實際生成 character/scene images。

## 7. 下一步

1. 繼續收 `20260819-1404-oda-case0005-status-go-009` immutable receipt。
2. 同時補 generic Go queue-owned fanout coordinator，先支援 0005-030/040。
3. 010 若 completed，立即用 reconciliation 推進 020；若 pending/running，只追 status；若 failed，只修真實 leaf blocker。
