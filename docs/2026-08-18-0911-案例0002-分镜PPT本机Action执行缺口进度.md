# 案例 0002：分镜 PPT 本机 Action 执行缺口进度

更新时间：2026-08-18 09:46（Asia/Taipei）

## 1. 当前产品流程

案例 0002 的权威顺序为：

```text
Director / storyboard plan
→ 0002-025 无配图 storyboard PPTX
→ 0002-027 用户审批
→ 0002-030/040/050 REAL 图片与绑定
→ 0002-055 带配图 storyboard PPTX
→ 0002-057 用户审批
→ 0002-060 REAL 视频
```

任何 IMAGE / VIDEO 都不得越过对应用户审批。

## 2. 大模型正确工作方式

大模型不预先记住下一工具，而是：

```text
用户要求继续案例
→ 查询 OpenWorker durable CaseWorklist
→ 读取 canonical_next_step_id / allowed_actions / acceptance
→ 查询 go-tool-runtime capability / operator guide
→ 取得 owning repo / workflow / canonical inputs / host / success criteria
→ 通过正式本机 Action 执行
→ 查询 target run 到 terminal
→ 验收 workspace PPTX / manifest / persistent action receipt
→ OpenWorker 原子写回 Worklist
→ 再查询下一步
```

当前 0002-025 应由模型从 Worklist 自己发现 `presentation.openmaic`。

## 3. 当前 main 的固定机器权威

2026-08-17 曾短暂实现 assigned-host shared queue / polling agent / spool executor / Windows persistent work-agent 服务，但该整套实现随后于同日被显式 revert，包括：

- assigned-host polling agent
- spool executor
- shared work queue provider
- local-work HTTP endpoints
- Windows assigned-host service installer
- ODA/O87 service bootstrap
- service-routed Case 0002 smoke

因此 **当前 main 不应继续依赖已撤销的 gtr-work-agent / gtr-work-executor 服务路径**。

当前有效执行面仍是正式 GitHub Action + self-hosted Windows；机器真实性由 persisted `assigned_host` 与真实 `COMPUTERNAME` fail-closed 校验。

OpenWorker 已存在 `OPS Patch Case 0002 Host Routing`，明确将 `runs-on: [self-hosted, Windows, X64, ODA]` 改成 `runs-on: [self-hosted, Windows, X64]`，保留 COMPUTERNAME 作为机器权威。因此 Case 0002 不应把 custom runner label 当唯一机器真相。

## 4. 已补 Worklist 与审批缺口

- 新增 `0002-025` 无配图 OpenMAIC PPTX。
- 新增 `0002-027` 无配图 PPT 用户审批。
- IMAGE 改为依赖 027。
- 新增 `0002-055` 带配图 OpenMAIC PPTX。
- 新增 `0002-057` 带配图 PPT 用户审批。
- VIDEO 改为依赖 057。

## 5. 已补 OpenMAIC durable evidence 缺口

OpenMAIC owning Action 现在不再只把最终 JSON 打在日志，而会在 canonical PPTX 同目录原子保存：

```text
*.pptx
*.manifest.json
*.receipt.json
```

receipt schema：`openmaic-presentation-action-receipt/v1`，包含：

- owning Action `run_id / run_attempt`
- artifact path / size / slide_count / sha256 / media_count
- manifest path
- runner `computer_name`

这让 OpenWorker 不需要从日志猜成果。

## 6. 已补 OpenWorker receipt → Worklist adapter

新增：

```text
scripts/case0002_apply_openmaic_receipt.py
```

adapter 会 fail-closed 验证：

1. tool/status 必须为成功的 `presentation.openmaic`。
2. receipt `action.run_id` 必须等于本次 target run id，禁止误吃旧 receipt。
3. runner `computer_name` 必须等于 durable Worklist assigned_host。
4. artifact / manifest 必须位于 workspace 内且实体存在非空。
5. 重算实体 PPTX SHA256，并与 receipt、manifest 对齐。
6. 重新打开 PPTX OOXML ZIP，统计真实 `ppt/media/*`。
7. receipt media_count 必须等于实体 media_count。
8. `0002-025` 必须 media_count = 0。
9. `0002-055` 必须 media_count > 0。
10. slide_count 必须大于 0，并与 manifest 一致。

CaseWorklistRuntime 新增 `accept_action_evidence(...)`，在同一 mutation lock 内完成：

```text
active ownership check
→ 写入全部 validated evidence
→ 清 active action/execution
→ acceptance completeness check
→ PASS
→ 单次 save
```

避免 evidence 半写入后 ownership/PASS 失败。

永久测试已经覆盖：

- 025 成功后 next 必须为 027；
- text-only PPT 出现 media 必须拒绝；
- receipt media_count 与实体 OOXML 不一致必须拒绝；
- receipt target run id 不符必须拒绝；
- 实体 SHA 被篡改必须拒绝；
- 055 必须有真实媒体。

当前这些代码已提交，但尚无本轮 CI/REAL run evidence，因此不把测试文件存在误报为 CI 已绿。

## 7. go-tool 0002 harness 已从 dispatch-only 升级为完整 closure

`cmd/e2e-0002-dispatch` 当前顺序：

```text
读取 D:\AI-Example\0002\.openworker\case-worklist.json
→ 取得 canonical next step / allowed action
→ 使用 OpenWorker case_worklist_action.py start 建立 active ownership
→ 查询 go-tool capability / readiness
→ /api/execution/dispatch 正式派 owning repo Action
→ 取得 target run id
→ /api/execution/runs/<id> 轮询到 terminal
→ target conclusion 必须 success
→ 找到 output 对应 persistent *.receipt.json
→ OpenWorker adapter 验证 expected target run id
→ 重验实体 PPTX / manifest / SHA / OOXML media
→ 原子 acceptance
→ 025 后强制 next=027；055 后强制 next=057
```

go-tool Operator E2E workflow 现在只在真正 ODA candidate 上 checkout OpenWorker authority；若 harness 失败，也只有在 active execution 明确属于当前 harness run/attempt 时才执行 `block-active`，避免误伤别的执行。

## 8. 已确认第一阶段 request evidence 形式

旧的 0002 REAL adoption workflow 明确将 `0002-020.storyboard_request` 写为绝对路径字符串：

```text
D:\AI-Example\0002\runs\31994303493\presentation\storyboard-request.json
```

因此当前 go-tool canonical input adapter 可以确定性转换为 workspace-relative request_relpath，不需要猜“最新 request”。

## 9. 当前真实阻塞

截至 2026-08-18 09:46，**仍然没有本轮机器 evidence 能证明 0002-025 已实际 dispatch 或生成 PPTX**。

GitHub Connector 当前能：

- 读取已知 run/jobs/logs/artifacts；
- rerun 已知 run/job；

但没有暴露“创建新的 workflow run / workflow_dispatch”的写动作。

本轮曾验证 Issue / issue_comment 作为替代控制面，但 Connector 创建的 repository event 没有启动 Actions；该实验 workflow 已删除、Issue 已关闭，不作为正式架构。

历史 OpenMAIC run `31978368819` 与 `31986406945` 均已核对为 CLI REAL gate，不是 `operator-presentation-generate.yml` owning Action，因此不能为了点火 025 而错误 rerun。

所以当前唯一未闭环的是**从本会话主动创建新的 Operator workflow run**；PPT 业务、durable receipt、物理 QC、Worklist acceptance 与审批停点已经补齐。

## 10. 下一 REAL 验收点

一旦正式 dispatch 入口可用，只跑当前 Worklist step：

```text
canonical_next_step_id = 0002-025
capability = presentation.openmaic
assigned_host = DESKTOP-ODAQN0D
request = 0002-020.storyboard_request
output = presentation/storyboard.pptx
```

必须取得：

```text
target Action conclusion = success
persistent storyboard.receipt.json.action.run_id = target run id
PPTX exists && nonempty
manifest.status = succeeded
slide_count > 0
physical SHA256 = receipt SHA256 = manifest SHA256
physical ppt/media count = receipt media_count = 0
```

随后 OpenWorker 原子 PASS `0002-025`，canonical next 必须变成 `0002-027`，然后停止自动生产并把无配图 PPTX 交给用户审阅。
