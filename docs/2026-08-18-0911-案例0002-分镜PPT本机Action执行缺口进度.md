# 案例 0002：分镜 PPT 本机 Action 执行缺口进度

更新时间：2026-08-18 10:18（Asia/Taipei）

## 0. 案例记录铁律

从本次继续执行开始，案例 0002 的每一个步骤都必须在本文件留下详实记录。不得只写“已修”“已跑”“PASS”。每个步骤至少记录：

1. 时间与步骤编号；
2. 当时从 OpenWorker 读到的 canonical next step、dependencies、allowed_actions、acceptance、assigned_host；
3. 向 go-tool 提出的实际问题/task；
4. go-tool `/agent/query` 返回的 capability catalog / selected / readiness；
5. 最终选择的 capability、owning repo、workflow、ref、canonical inputs；
6. 本机 Action run id / job id / run attempt / COMPUTERNAME；
7. 实际工具输出、workspace artifact 路径、大小、SHA256、manifest/receipt；
8. 实体 reopen/QC/OOXML media 等验收；
9. OpenWorker 写回的 evidence、PASS/BLOCKED 与下一 canonical step；
10. 若失败：原始错误、真正 owning layer、修复 commit、永久测试、同一路径重跑结果。

任何没有机器证据的动作必须明确标为 `IMPLEMENTED / NOT LIVE VERIFIED`，不得写成 REAL PASS。

## 1. 当前产品流程

案例 0002 的权威顺序为：

```text
Director / storyboard plan
→ 0002-025 无配图 storyboard PPTX
→ 0002-027 用户审批
→ 0002-030/040/050 REAL 图片与绑定
→ 0002-055 带配图 storyboard PPTX
→ 0002-057 用户审批
→ 0002-060 REAL 视频
```

任何 IMAGE / VIDEO 都不得越过对应用户审批。

## 2. 大模型正确工作方式

大模型不预先记住下一工具，而是：

```text
用户要求继续案例
→ 查询 OpenWorker durable CaseWorklist
→ 读取 canonical_next_step_id / allowed_actions / acceptance
→ 调用 go-tool-runtime POST /agent/query
→ 读取 Action capability directory / readiness / prompt
→ 对 OpenWorker allowed action查询具体 capability
→ 取得 owning repo / workflow / canonical inputs / host / success criteria
→ 通过正式本机 Action 执行
→ 查询 target run 到 terminal
→ 验收 workspace PPTX / manifest / persistent action receipt
→ OpenWorker 原子写回 Worklist
→ 再查询下一步
```

当前 0002-025 应由模型先从 OpenWorker 取得任务语义，再通过 go-tool-runtime 查询确认正式工具，不能靠模型记忆或案例代码直接猜工具。

## 3. 当前 main 的固定机器权威

2026-08-17 曾短暂实现 assigned-host shared queue / polling agent / spool executor / Windows persistent work-agent 服务，但该整套实现随后于同日被显式 revert。因此当前 main 不应继续依赖已撤销的 gtr-work-agent / gtr-work-executor 服务路径。

当前有效执行面仍是正式 GitHub Action + self-hosted Windows；机器真实性由 persisted `assigned_host` 与真实 `COMPUTERNAME` fail-closed 校验。

## 4. Worklist 与审批链

当前模板 revision = 3，workspace=`D:\AI-Example\0002`，assigned_host=`DESKTOP-ODAQN0D`。

关键门：

- `0002-025`：Generate text-only storyboard PPTX through OpenMAIC；allowed action=`presentation.openmaic`；acceptance=`storyboard_pptx/storyboard_manifest/storyboard_pptx_sha256/slide_count/reopen_receipt/image_count`。
- `0002-027`：用户审批无图 PPT；IMAGE 必须等待该步骤 PASS。
- `0002-055`：带图 PPT。
- `0002-057`：用户审批带图 PPT；VIDEO 必须等待该步骤 PASS。

## 5. OpenMAIC durable evidence

OpenMAIC owning Action 现在应在 canonical PPTX 同目录原子保存：

```text
*.pptx
*.manifest.json
*.receipt.json
```

receipt schema：`openmaic-presentation-action-receipt/v1`，包含 owning Action `run_id/run_attempt`、artifact path/size/slide_count/sha256/media_count、manifest path、runner computer_name。

## 6. OpenWorker receipt → Worklist adapter

`scripts/case0002_apply_openmaic_receipt.py` fail-closed 验证 tool/status、target run id、COMPUTERNAME、workspace containment、实体文件、SHA256、manifest、PPTX OOXML ZIP、真实 `ppt/media/*`、slide_count；025 要求 media=0，055 要求 media>0。

CaseWorklistRuntime `accept_action_evidence(...)` 在同一 mutation lock 内完成 ownership check → 全部 evidence → clear active → acceptance completeness → PASS → save。

## 7. go-tool `/agent/query` 关键缺口与修复

真实案例发现：原 `/agent/query` 虽存在，但 information pack 原本没有 go-tool 自己注册的 GitHub Action capabilities，因此模型“问了 go-tool”仍可能看不到 `presentation.openmaic`、`comfyx.queue.admin`。

已补 Action capability directory：

```text
action_capabilities.catalog
action_capabilities.selected
```

prompt 同时加入 `<ActionCapabilityDirectory>`，要求模型只能从已注册 capability 选择，不确定时继续查询具体 capability，不得猜工具或绕过 owning repo。

## 8. 0002 Operator 机器证据顺序

```text
00-openworker-current.json
→ 00a-runtime-health.json
→ 01-agent-query.json
→ 02-capability-detail.json
→ 03-readiness.json
→ 04-dispatch-request.json
→ 05-dispatch-receipt.json
→ 06-status-terminal.json
→ 07-openworker-accepted.json
→ 08-result.json
```

`01-agent-query.json` 是硬 gate：必须证明先查 go-tool，再允许执行。

## 9. go-tool 0002 harness closure

当前设计：读取 durable Worklist → /agent/query → capability detail/readiness → OpenWorker start active ownership → `/api/execution/dispatch` → target run id → poll terminal → success → persistent receipt → OpenWorker adapter 实体验收 → atomic acceptance → 025 后必须 next=027；055 后必须 next=057。

## 10. 第一阶段 request evidence

旧 REAL adoption workflow 将 `0002-020.storyboard_request` 写为绝对路径字符串，例如：

`D:\AI-Example\0002\runs\31994303493\presentation\storyboard-request.json`

因此 canonical input adapter 可确定性转换为 workspace-relative request_relpath，不猜最新 request。

## 11. 2026-08-18 10:18 本轮执行日志

### 11.1 用户要求

用户要求继续案例，并新增铁律：**每个步骤都要详实记录到案例文档**。本节开始执行该要求。

### 11.2 重新读取案例模板

读取 `case-worklists/0002.json`：revision 3；assigned host `DESKTOP-ODAQN0D`；模板中 010/020/025... 均为 PENDING。注意：这是 Git 模板，不等同 ODA workspace durable runtime 状态；不得拿模板 PENDING 覆盖本机已通过 evidence。

当前产品预期仍是：durable 010/020 已有历史 REAL adoption evidence，下一生产目标应为 025；但在拿到 ODA durable `00-openworker-current.json` 前，此处只记为“预期”，不能冒充本机 current-state 证据。

### 11.3 REAL 点火调查

目标：找到与当前 `.github/workflows/operator-e2e-0002-comfyx-studio.yml` 相同 harness 的历史 run/job，利用 GitHub Connector 已有 rerun 能力安全点火当前路径。

已核对历史提交 `77c4a5c...`：该 workflow 使用 `GITHUB_RUN_ID + GITHUB_RUN_ATTEMPT` 锁，证明 rerun attempt 被设计为合法重跑路径；历史提交 `b5a7c193...` 将 concurrency 设为 `cancel-in-progress: true`，目标是只保留最新 formal dispatch harness。

当前限制：GitHub Connector 的 commit workflow-runs 查询只枚举 PR-triggered runs；通用 fetch 又不允许列 Actions runs endpoint，因此尚未取得该 Operator harness 的历史 run/job ID。没有确认 workflow 身份前，禁止拿旧 Studio source-to-film 或 OpenMAIC CLI gate run 来 rerun。

状态：`BLOCKED AT CONTROL-PLANE DISCOVERY / PRODUCT STEP 025 NOT STARTED`。

### 11.4 当前不得误报的事项

- 没有新的 `01-agent-query.json`，所以不能声称本轮模型已在 ODA 实际问过 go-tool。
- 没有新的 target run id，所以不能声称 OpenMAIC owning Action 已启动。
- 没有新的 storyboard PPTX/manifest/receipt，所以 025 不能 PASS。
- 当前所有 Action capability / adapter / harness 修改均属于 `IMPLEMENTED / NOT LIVE VERIFIED`，直到取得本轮机器 evidence。

## 12. 下一 REAL 验收点

只允许沿同一路径继续：

```text
ODA durable Worklist current
→ go-tool /agent/query
→ registered allowed Action
→ capability detail/readiness
→ 0002-025 owning Action
```

必须取得：

```text
01-agent-query.json exists
query catalog contains presentation.openmaic
target Action conclusion = success
persistent storyboard.receipt.json.action.run_id = target run id
PPTX exists && nonempty
manifest.status = succeeded
slide_count > 0
physical SHA256 = receipt SHA256 = manifest SHA256
physical ppt/media count = receipt media_count = 0
```

随后 OpenWorker 原子 PASS `0002-025`，canonical next 必须变成 `0002-027`，然后停止自动生产并把无配图 PPTX 交给用户审阅。
