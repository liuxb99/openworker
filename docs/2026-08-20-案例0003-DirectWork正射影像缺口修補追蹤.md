# 案例 0003 玉井橋 — DirectWork 正射影像缺口修補追蹤

- 日期：2026-08-20（Asia/Taipei）
- Case：`0003` / 玉井橋
- 固定機：`DESKTOP-UL7V2VV`
- Workspace：`D:\AI-Work\jobs\0003-YUJING-BRIDGE`
- 新版 authority：`liuxb99/DirectWork`
- Query/context：`liuxb99/go-tool-runtime`
- Owning repo：`liuxb99/Terrain_To_DXF`
- 本輪密語：`CASE0003.ORTHOPHOTO.CONTINUE`
- 本輪 request：`case0003-orthophoto-20260820-a2`

## 本輪新增發現

第一版 Case 0003 DirectWork 密語 ingress 已能建立 `/v1/work` command envelope，但其 business command 原本在本機缺少 `Terrain_To_DXF` checkout 時嘗試跨 private repo clone。這不是可靠的 local-first runtime 前提：DirectWork repo 的 GitHub transport token 不應被假設具有另一 private repo 的讀取權限。

因此這不是 orthophoto producer 本身的缺口，而是 **source dependency / credential boundary** 缺口。

## 已補修

DirectWork branch：`case0003-directwork-orthophoto-20260820`

主要修補 commit：`782c0056f768e18e28400aed46e8bc0e830d074a`

修法：

1. 不再把跨 private repo `git clone` 當成 durable work 前提。
2. DirectWork executor 在 UL7 fail-closed 探測既有 `Terrain_To_DXF` checkout。
3. 候選 checkout 必須同時存在 `.git`、`go.mod`、`cmd\terrain-orthophoto-acquire`。
4. 找不到就 terminal failure，並列出全部 checked paths，方便下一輪補真實安裝/路徑缺口。
5. 找到後記錄 `tool_root` 與 `producer_commit`。
6. 執行 `go test ./...`、build producer、NLSC PHOTO2 bounded acquire、JPEG/evidence/SHA256 驗收。
7. go-tool probe 由 `/health`、`/tools` 擴充到 `/projects`，但仍只作 query/context evidence。

## request a2

已將密語 request 更新為：

```text
request_id = case0003-orthophoto-20260820-a2
phrase = CASE0003.ORTHOPHOTO.CONTINUE
```

這次 request 的唯一目的，是取得 UL7 DirectWork durable evidence，不是驗證 GitHub workflow 本身。

## 下一個實機 checkpoint

正式觸發後必須同步以下實際值：

```text
github_transport_run_id
work_id
created_at / claimed_at / started_at / finished_at
events
slot / executor / pid
status
exit_code
go-tool probes
tool_root
producer_commit
artifact path / size / SHA256
producer evidence
secret receipt
```

若失敗：

- `Terrain_To_DXF local checkout missing` → UL7 本機 tool installation/path registration gap；不可回退舊 GitHub operator 冒充 authority。
- build/test failure → 修 `Terrain_To_DXF` owning repo。
- 沒有 claimed/running → 修 DirectWork queue/slot/runtime。
- artifact 有但 SHA/evidence 不符 → 修 producer acceptance。
- 只有 fresh orthophoto durable PASS 後，才能接 Drive verified publish 與 ChatGPT exact-image review。

目前狀態：**ingress runtime dependency gap 已補；request a2 已準備；尚未讀到 `secret-results/case0003/case0003-orthophoto-20260820-a2.json` 前，正射與 ChatGPT review 均不得標 PASS。**
